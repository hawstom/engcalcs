# Closed-task ID ledger

One line per closed task, newest first. **This is an index, not a record** — it exists so that a
`Task N` cited from code or from a `dev/` doc still resolves to something, and so
`roadmap_id_check.php` can prove an ID is used once.

The full text of every closed task is in git and nowhere else, by Tom's ruling of 2026-08-16:
`git show 5083c95^:dev/roadmap-closed-archive.md` for the summaries, and
`git log -p -- dev/roadmap-closed-archive.md` for the original narratives behind them.

Durable policy from these tasks lives in `CLAUDE.md` and in `dev/*.md`. Those are the current
state; this is only a list of numbers that were once used.
- 0|558| A finger could tap a node but could barely grab one to drag it, closed 2026-08-30 (Tom: *"for some reason dragging a node can be very difficult"*). **`touchAssetNear()` — Task 417's 24 px forgiveness — had exactly ONE call site, and it was the pointerUP handler.** The pointerDOWN handler, which starts a drag, called `mapHitAt()` and nothing else, so a tap was forgiving to 24 px while a grab needed the drawn dot plus `HIT_SLOP_PX` = 2. `settings.symbolSize` defaults to 7 screen px, so **the whole grab target was a disc about 11 px across** — a ~5 px accuracy demand on a fingertip, which a mouse never feels. **This is almost certainly what Tom's withdrawn long-press request was reaching for**: he could not reconstruct why he had wanted it, and a long press is what you ask for when you cannot say *this one* by pointing. Fix the grab and the want dissolves. **The rule: a finger grabs a node at the POINTER's radius (14), not the tap's (24)**, in its own constant `NODE_GRAB_TOUCH_PX` because a feel gets re-tuned. The asymmetry is deliberate and the reason is that the two failure modes are not equal: a tap on bare map does nothing, so a promoted near miss costs nothing, but a press on bare map PANS — so every pixel of grab radius is a pixel where panning stops working, and a grab that misses merely pans and is retried while a pan that steals a node MOVES THE DOCUMENT. **The consequence is stated in an assertion so nobody fixes it by accident: at 20 px from a junction a tap opens it and a press pans.** Rejected and recorded: deciding on MOVEMENT — a pan and a node drag are the same gesture, press then travel, so there is no later moment at which they differ and any threshold would be a guess dressed as a measurement. Nodes only: labels are drawn text and a pipe is a stroke with a halo, both already finger-sized, and a grab corridor down every pipe is laid exactly where a pan most needs to start. **Task 417's original residue is CLOSED with evidence rather than assumed**: `selectFromHit(t)` runs unconditionally on pointerdown, the promotion was placed above it so the promoted node is the one selected, and the harness asserts `selectedRef()` is that node immediately after the press and before any movement. **And the harness found a stub defect of the exact shape `dev/testing-notes.md` warns about:** `lpn-dom-stub.js` did not link `setAttribute('data-node', …)` to `element.dataset.node`, which the real DOM keeps as one storage — so a drawn node was a hit to `getAttribute()` and BARE MAP to every pointer handler, all of which read `t.dataset.*`. Taught the stub the relationship rather than asserting around it. `dev/lpn-spike/touch-radius-harness.js` §4.
- 0|559| The symbology label tables grouped each control set with the WRONG row, closed 2026-08-31 (Tom: *"the toggles and labels in the Node and Link symbology have a little gap below them from the row they belong with, but no gap above them from the row they don't belong with. This would be good to reverse."*). **One inline `row.style.gap = '6px'` written by `labelCheckbox()` was doing two jobs.** Where the row does not wrap it is purely a COLUMN gap; the moment it wraps — name on line 1, the four columns on line 2 — the same declaration becomes a ROW gap as well, and it lands INSIDE the group, between a name and its own boxes. The row `div`s carried no margin at all, so **every pixel of separation in the list was inside a group and none of it between groups**: the classic proximity defect, whitespace grouping controls with the neighbour they do not belong to. Measured in Chromium at 360x740: 0.00 px above, 6.00 px below; after, 6.00 above and 0.00 below. The 6 px was MOVED, not added to, in CSS only (`row-gap: 0 !important` to beat the inline shorthand, plus `margin-top` on `:not(:first-child)`); `column-gap` untouched, so nothing widened. **Applied in both places the row wraps** — the `@container lpnset (max-width: 24rem)` block and the 640px phone block — so a browser without container queries still gets it, and a desktop user who drags the box under 24rem gets it too. **Not a desktop defect**: at 1280 the rows are `nowrap`, so there is no row-gap to reverse and the desktop measurement is identical before and after. `:not(:first-child)` was chosen over `div + div` deliberately, and the reason is in the CSS: the sibling combinator is the one selector shape `small-screen-harness.js`'s reader cannot parse, so the rule would have landed in that file's blind-spot report and gone unasserted. **The harness asserts the COMPARISON, not either number** — space above exceeds space below, so the plausible tidy-up drift of `6px above / 6px below` fails — and it first asserts that `labelCheckbox()` still writes an inline gap at all, without which the rest is vacuous.
- 0|560| A finger that dragged a node also opened its editor, closed 2026-08-31 (Tom: *"sometimes the node editor comes up when I am trying to drag, sometime after a successful drag"*). **The two halves of one press disagreed about what a press IS.** The tap test on `pointerup` was a flat `hypot(...) >= 4` on the FINAL displacement and blind to pointer type, while `applyDrag()` moved a node on the FIRST pixel — `pointermove` set `dragDirty` unconditionally. So a finger that grabbed a junction and slid it a short way did BOTH: moved the node and opened the editor. **Task 558 did not introduce the flat 4 — it made it reachable.** Run against `fcc2b572^`, a touch press 12 px off a node with a 3 px slide gave `drag: pan, popup: true, node unmoved`; after 558 the same gesture gives `drag: node, popup: true, node MOVED`. Before 558 a finger on bare map never started a node drag, so *"after a successful drag"* was unreachable that way; *"when I am trying to drag"* existed before, via a short pan. **The rule, at `TAP_MOVE_PX`: a press becomes a drag only once it has travelled past its own slop — 4 px for a pointer, 10 px for a finger — and until then NOTHING MOVES; past it the gesture is a drag and can never also be a tap.** Three parts: a `gestureMoved` flag armed in `pointermove`, `dragDirty` withheld until it arms (so a sub-slop press nudges nothing rather than nudging and then also opening), and the tap test reading that flag plus `tapMovePx(e)`. The press offsets are kept, so an arming drag does not jump — a 20 px slide moves the node 20, not 10. The pointer's 4 is untouched, so no mouse gesture changes. **A second stub defect of the shape `dev/testing-notes.md` warns about, caught while writing the guard:** the harness exported `applyDrag: function () { if (drag) { applyDrag(); } }`, which removes the very coupling this defect lives in — the page calls `applyDrag()` from `tick()` only when `dragDirty` is set, and the whole fix is a `pointermove` that DECLINES to set it. The export is `tick()`'s body verbatim now; without that, the new section would have passed for the wrong reason. `touch-radius-harness.js` §5.
- 0|561| The node editor sometimes opened with its pattern list already showing, closed 2026-08-31 (Tom, on a phone). **NOT REPRODUCED HEADLESSLY, AND THE FIX IS REASONED RATHER THAN MEASURED — read that before trusting it.** Ruled out by reading, each of which would have been deterministic: nothing on the node-popup path calls `focus()`; the pattern `select` has no `size` and no `multiple`; `libFillPatternOptions()` rebuilds every option and never opens the control; and no state leaks from the previous popup, because `renderNodeFields()` calls `clearFields()`, which wipes `innerHTML`, so the select is a new element every time. That leaves the browser's **compatibility ("ghost") click**: a touch tap fires pointerdown/pointerup and THEN a mousedown/mouseup/click at the same screen point. The popup opens synchronously inside the pointerup and on a phone is nearly screen-width, clamped beside the tapped junction — so the point the finger just left is very often inside it a millisecond later, and a `select` receiving that click opens its picker. It is the only candidate consistent with *sometimes* and with it being the pattern control specifically. `ghostClickShield()`: a popup opened by a finger takes `pointer-events: none` for 350 ms, one opened by a mouse never does, and `closePopup()` drops the shield on the same line it calls `hideTipsIn` — that function's history from Task 555 being exactly why. Rejected and recorded: `preventDefault()` on the ghost click, because a `select` opens on the MOUSEDOWN of that sequence, one event too early; and moving the popup away from the finger, because on a 390 px screen there is no away. **No harness can hold this** — a compatibility click is generated by the browser, so the harness holds the SHIELD and not the defect. A phone tap on several junctions in a row is the real test.
- 0|563| A label you could not see could still be grabbed, and dragged off the screen, closed 2026-09-01 (Tom: *"somehow a node label is present but not (yet?) visible, and when I go to pan, the label that I did not see drags off the screen"* — a bug he said he had a hard time isolating). **`.lpn-draglbl` carried `pointer-events: all`, and SVG's `pointer-events` has two families:** `visiblePainted`/`visibleFill`/`visibleStroke`/`visible` require `visibility: visible`, while `painted`/`fill`/`stroke`/`all` **deliberately ignore visibility**. So a hidden label kept a full hit target — and **the stylesheet comment beside the hiding rule asserted the opposite**, that `visibility:hidden` also drops pointer events, which is why nobody looked there. Measured in Chromium by sweeping a hidden `text` element's own box against `elementsFromPoint()`: under `all`, **100% of the box is still a hit**; under `visible`, 0%. A SHOWN label's grabbable area is identical either way, so nothing became harder to pick up. Four separate mechanisms hide a label and all four were affected (`.lpn-labels-hidden` for thematic colouring and the georef wizard, `.lpn-lbl-hidden` for a Text off in this scenario, and `setLabelAssemblyHidden()`'s inline style for a label the collision pass dropped or yielded); node, link and Text labels all wear `.lpn-draglbl`, so one keyword fixes three. **The rule, enforced by construction: a thing the user cannot see cannot be grabbed** — `pointer-events: visible` IS `all` gated on visibility, so invisibility and un-grabbability are literally the same fact rather than two that can disagree. `mapHitAt()` gained `hitIsVisible()` as the same fact read from JS, and it `continue`s rather than returns, so rejecting a ghost still reaches the node underneath it. **`visiblePainted` was the obvious one-word fix and is wrong**: for text it drops the gaps between glyphs, and a finger is not aimed at the inside of an "8". `all` is now absent from the stylesheet entirely, which lets the lint be absolute. Guards: `hidden-label-grab-harness.js` (17, verified to produce 7 failures with the guard disabled) and `dev/browser-pass/specs/hiddenlbl.js` §39 (13, real Chromium). **The third stub defect in three days, and it was predicted in the brief:** `lpn-dom-stub.js`'s `elementsFromPoint()` returned `[hitTarget]` — one element — so it held the second item of the stack constant at *absent*, making the whole question of whether rejecting the top reaches the thing beneath unaskable, on the very map where a node's label is drawn over its node. It takes a stack now.
- 0|564| A moved junction now says so, closed 2026-09-01 (Tom: *"we have no way of knowing when a junction moves. Some sort of a fading highlight like the labels have when they are moved would help. And this doesn't apply only to the phone, of course."* — and, on why it matters more than it looks, *"the blindness of tapping on a phone is dire"*: a finger covers what it touches, so the visual confirmation is the only confirmation). **`markJustDragged()` gained a second caller rather than a second implementation** — the label mark Tom asked for on 2026-08-15, one class and one self-clearing CSS animation, no timer and no object. Only the PAINTING differs, and it had to: `fill` is where the colour ramp writes inline, so a 45-second red dot would be **a pressure reading that is not true**, and a reservoir's disc is `fill: none`. So the node keyframes paint a red HALO RING, `paint-order: stroke fill` making it a halo rather than a blot so the node keeps its own colour. **Every declaration is inside the keyframes so it all reverts** — nothing removes the class, and a width left in the rule would have permanently altered the selection ring. Width is `calc(3 * var(--lpn-sym))`, the world-unit variable every other ring uses: `stroke` alone and a `drop-shadow` both carry a width in screen units, and the stylesheet's own comment records a mark bigger than the screen produced twice from that mistake on a lat/lon map. **Size was checked by screenshot, not arithmetic** — 0.7x the symbol is technically correct and visually a hairline. Fires once at `pointerup`, gated on `drag.snapped`, which already means *this drag wrote to the document*, so a tap, a press inside the slop and a pan cannot reach it. **Undo fires it too**, that being the one other place a junction moves with no pointer on it: positions are read before `doc` is replaced and compared after `buildDom()`, and only ids present on both sides count, because a node an undo RESURRECTS has not moved. Links deliberately not marked — a vertex drag leaves the bend under the pointer, and a node move already redraws its pipes. `prefers-reduced-motion` disables both by name. It is a class on an element, never in the document; asserted absent from `serializeProject()`. `node-moved-mark-harness.js` (24).
- 0|540| Find and replace with a typed query language, closed 2026-09-01 — built, parser and all, and Tom: *"This is already right."* A `Connection` property whose conditions are the four faults in HIS words and HIS order (`no links`, `no open links`, `no link path to a source`, `no open path to a source`), and the query as an INPUT above the Find button that parses back — AND, OR, brackets, translated operators with the English words accepted as aliases in every language. **His frame is the one that made it work:** *"I see two points, sources and this node. And I see either no connection (missing link) or no open connection."* Two points x two kinds of break. **They NEST**, each row the one above plus one more way to be cut off, which is what a searcher wants — pick how wide to cast. **The PARTITION was the mistake and it was AI's:** the first build made them mutually exclusive buckets and presented him with four cases he did not recognise (*"you are continually presenting four cases that I do not see"*). Do not restore it. `lpnDiagnose()` returns ONE code, `unreachable`, for all three faults; the panel splits it and the harness asserts the three together are exactly `unreachable`. The first two conditions answer on a HALF-DRAWN network; the last two need a source and, with none, match NOTHING rather than everything — source-lessness is one fault of the NETWORK, not N faults of the nodes. A result row prints the NARROWEST condition true of that node. The extremes became a standard condition on his ruling (*"a control that appears for some properties and not others teaches nobody what it is"*), ranking alphabetically on text, and `Highest n` became `n highest` because that says what the Value box holds. **When the typed query outruns the controls the controls LEAVE**, and one button brings them back to the last query they did express — showing the first condition of a compound query, or greying stale words, both claim a search that is not the one about to run. Deliberately not built: `NOT`, and comparing one property with another. `find-harness.js`, 132 assertions. **Open only as a question, never blocked:** the printed line reads `Pipe.Velocity highest 10` because that is the form the parser reads back; flipping it is a parser change, so ask first.
- 0|417| A finger's tap radius, and the long press that turned out to be a symptom, closed 2026-09-01. **The half that was real shipped 2026-08-28**: `NODE_SNAP_TOUCH_PX` 24 — a 48 px target, the figure Apple's and Google's guidelines both settle on — against the pointer's unchanged 14, `tapSlopPx(e)` picking per press from `e.pointerType`, and **a PEN taking the POINTER's number**, because it is precise and it is what a surveyor marking up a tablet is holding. The half Tom's own parenthesis named was the one nobody had noticed: the add-* branches had always had a screen-pixel snap and **SELECT mode never did**, so tapping a junction and missing its dot by five pixels opened nothing, which a user cannot tell from a press the page dropped. **NOT asserted at 360px, deliberately** — the radius follows the POINTER, not the screen, so a width-keyed assertion would prove something the code does not do. **The long-press half was requested, then withdrawn, then explained.** Tom asked for it, could not later reconstruct why (*"I can't remember exactly why I identified that we might want long press"*), and struck it on a principle worth keeping — *"we don't want non-parallel UX"*, one action must not have two gestures, and long press already means something else here (`PROFILE_HOLD_MS`, the path chooser). **Then the real defect surfaced:** a finger could TAP a node at 24 px but had to GRAB one at 2 px plus a 7 px symbol to drag it. **A long press is what you ask for when you cannot say *this one* by pointing** — fix the grab and the want dissolves, which is what happened (closed Task 558). The original framing's residue — whether a touch drag leaves the element selected — was CLOSED WITH EVIDENCE rather than assumed: `selectFromHit(t)` runs unconditionally on pointerdown, the grab promotion sits above it so the promoted node is the one selected, and the harness asserts it before any movement.
- 0|556| The Settings box left a blank strip under its content once it had been capped, closed 2026-08-30 (Tom: *"The phone settings has a limit to the amount of box height it will use. This leaves a blank area at the bottom if the box is expanded taller."*). **The limit was RIGHT — Task 554's cap working as intended — and the blank under it was not.** A measurement-order bug in `capPanelHeight()`: the panel's own furniture is derived by subtraction, `naturalH − body.height`, which is valid only while both numbers describe the SAME layout. The function wrote `panel.style.maxHeight` FIRST, re-laying the panel out; the body is `flex: 1 1 auto` so it had already shrunk with the cap, and the subtraction then counted **the overflow the cap had just removed as though it were furniture**. The body was pinned `(naturalH − avail)` px too short, permanently, and no later resize could fill it. Measured in real Chromium at 360x640: body pinned to 326 px where the honest figure is 432.4, leaving **106.4 px of blank** against a content pane whose `scrollHeight` was 5,224 — so it was never "the content is just shorter than the box". `capPanelHeight()` is reached by EVERY capped panel — the property popup and Notes through `fitPanelToViewport()`, Find/Settings/Libraries/fire-flow through `capPanelToRoomBelow()`, the menus through `panelPlacement()` — so the fix is one function and correct for all of them. `small-screen-harness.js` §12 extracts the real function and reproduces the browser's exact numbers when the two statements are swapped back.
- 0|557| The phone Settings index pane took a fifth of the main pane's width, 2026-08-30, on Tom's instruction — and the instruction had to be corrected once, which is the part worth keeping. He wrote *"giving the index pane 20% of the main pane's width"* and it was first read as a TARGET RATIO (`index/content = 0.20`); he meant a **TRANSFER**: *"rob 20% of the then-present width of the main pane and give that to the index pane."* The two readings are opposite in effect — the transfer gives the index **119.7 px**, the ratio would have given **51.7 px**, *narrower than it already was*. `flex-basis: calc(3.6rem + 20% - 2px)`, derived from `4.5rem + 0.2(P − g − 4.5rem)`; the `−2px` is 20% of the gap and is exact, because a percentage flex-basis resolves against the flex container's inner width with `gap` NOT deducted — verified in Chromium, not trusted from the algebra. Phone only; the Libraries box needs no separate expression because the phone rule already overrides its desktop `5.25rem` (a `:has()` selector takes its argument's specificity, so `(1,1,1)` beats an id's `(1,1,0)`), making its present phone width 4.5rem too. **The harness asserts the TRANSFER — that the main pane keeps 80% of what it had — and names the rejected ratio reading**, because a ratio assertion would pin the wrong property and pass while the layout drifted. **Two measured consequences:** the narrowed content pane overflowed by 17 px and the sole offender was the colour-ramp credit's **bare URL**, one unbreakable token no pane width would have fixed (`overflow-wrap: anywhere`, phone only); and the browser pass's mid-word counter went from **8 of 14 index names breaking mid-word to 0 of 14** — the wider index removed the wrapping Task 527's tab strip was invented to work around. `dev/browser-pass/specs/smallscreen.js` had a hard `content > 230` assertion that this deliberately breaks; it is demoted to a note, keeping `!contentSideways`, the property the 230 was ever a proxy for.
- 0|554| The Settings and Libraries boxes hung off the bottom of a phone with the resize grip off screen with them, closed 2026-08-30. Tom: *"the box runs off the screen, impossible to resize because the control is inaccessible."* **The trap and the escape hatch were the same corner** — the grip is the box's last 28 px. **It is JS, not CSS, and not the fire-flow box:** 701 CSS lines changed since the touch-grip commit `1b7a44e5` and not one is a declaration on `.lpn-setbox*` or `.lpn-resize-grip`. The cause is `6d8a972c` ("Lat before lon everywhere", 2026-08-24), which gave `clampPanel()` a `topMin` of `chromeFloor()`: the top became `max(floor, min(top, vh − h − EDGE))`, and a box of `min(46rem, 92dvh)` on a 640 px screen wants a top of 47 px while the chrome floor is ~90, so **the floor wins and the box hangs off by the height of the chrome**. That is also why the `ResizeObserver` never slid it back: it re-runs the same clamp, gets the same top, and `at.left !== r.left` is false. The observer was never broken — the arithmetic it consults had no answer that fits. **AND THE FIX ALREADY EXISTED AND HAD BEEN APPLIED TO ONE SIBLING ONLY:** `capPanelToRoomBelow()` was written for exactly this on the Find box under Task 549 (2026-08-27), and the fire-flow box got it at birth — which is why the NEWEST box was the only one that behaved. `openSettingsBox()` and `openLibraryBox()` now cap before placing, the overflow going inside the scrolling `.lpn-popover-body`; and the `ResizeObserver` no longer stores a height the CAP produced, or opening the box once on a phone would ratchet the remembered desktop height down for ever. `small-screen-harness.js` §12 extracts the real `clampPanel` and runs it against the box's geometry resolved from the stylesheet at 360x640 — verified to produce 3 FAILs with the cap removed. **Not reproduced and deliberately not touched:** Tom also reported the index column narrower and the content pane wider than needed. No code change lies behind either — both figures are the ones `1b7a44e5` set. Changing them would be redesign, not repair.
- 0|555| A `?` glyph raised the phone keyboard, and a tip outlived the box that opened it, closed 2026-08-30 (Tom: *"it puts me in the input field, bringing up my input keyboard when I am not ready for any input"*; *"Tips in the Node editor survive the editor box on close on a phone"*). **The keyboard comes from the `<label>`'s activation behaviour on the click, not from the tooltip trigger** — `unitNumberField()` builds a native `<label>` wrapping both the `.ec-help`/`?` span and the `<input>`, and nothing cancelled the default. **That is why a uniform long-press trigger is not a fix**, though it was proposed as the structural one: under it a short tap still clicks, still activates the label, still raises the keyboard, and now shows no tip either — it would have removed the tip and kept the bug. Cancelled on `.ec-tip` (the glyph) and NEVER on `.ec-help`, because the two nestings are opposite and a fix on `.ec-help` would swallow the label TEXT's tap in the tip-only case, where tapping a field's name is meant to reach the field. Touch only. What stops the next `<label>`-wrapped control reintroducing it: the fix lives in `initTips()`, the one seam every tip on every page passes through, keyed on the glyph class both PHP helpers and `setFieldLabel()` emit — so the remaining gap is hand-assembled markup, which `tip_markup_check.php` already blocks. The second defect: `closePopup()` hid the popup but never called `hideOpenTips()`, and Bootstrap parents a tooltip to `document.body`. `hideTipsIn(root)` is now called by all six box closers, **scoped to the closing box** so closing one panel cannot sweep a tip someone is reading over another. `dev/lpn-spike/tip-behaviour-harness.js` (20) models the two couplings that matter — a label activates its control unless cancelled, a tooltip lives in the body — and was verified to produce 5 FAILs without the fixes.
- 0|542| Terrain elevations became two ordinary controls and the menu row was deleted, 2026-08-28, on Tom's own instruction. He had written the defect himself after pressing it: *"To be honest, I did not expect nor necessarily welcome what I got. I was just a dilettante pushing a cool new button that I found."* **(a) On creation:** `Settings > New assets > Elevation from`, and a node born on a geographic project is born BLANK — not at the typed default, because the fill refuses to overwrite a number that is already there, and 0 is sea level, which is a claim. A burst of drawing is ONE batch (600 ms after the last node, the shape `scheduleReshed()` uses): six junctions cost one tile request, not six, so the consent-gated third-party call is never on the critical path of a drawing gesture. Quiet — no progress line, no success line — but never silent about a failure or a node left blank. **(b) On an existing set:** `From Mapbox DEM` as the New-value source in Find and replace, offered only for Elevation, cleared by `replaceNormalize()` the instant the property moves off it. It writes nothing itself and takes no snapshot: `terrainFillElevations()` is the one write and owns the single undo step, and it reports what was ASKED FOR because the tiles are still on the wire. **(c) The Map-menu row is gone; do not restore a third door.** `EngCalcs.lpnTerrainFill()` stays as the list-deciding code its harness exercises, with no caller on the page. The enabling refactor is `lpnTerrainFillFor(points, opts)` — everything from the consent gate down, taking the nodes as an argument — so three doors are one behaviour rather than three. **The consent gate has no opt-out on any door**: what it protects is that a node coordinate says where the user's network IS, which is true however the fill was started. `dev/lpn-spike/terrain-harness.js` §6 (16). **A defect found on the way, unrelated and worse:** `lpn_labels_col_drop` was translated into all 26 languages and had never been wired into `Looped-Network.php`'s pageConfig block, so the Labels panel's "Drop" heading was English everywhere. `pageconfig_check.php` cannot see it — it matches literal `EngCalcs.pageConfig.<key>` reads, and that file's JS reads through the alias `pc`, making all 838 keys invisible to it. Found by diffing every `pc.<key>` read against the block by hand. That is a Task 322 survey row waiting to be written. **[H] Two keys are now rendered by nothing and were deliberately NOT deleted**: `lpn_terrain_menu` and `lpn_terrain_menu_tip`, the vanished row's label and tip, translated into 26 languages. Whether that is debt or lost content is Tom's call, not a script's — and **`key_hygiene_check.php` does NOT list them**, which is the part worth knowing: `EC.lpnTerrainMenuLabel()` and `EC.lpnTerrainMenuTip()` still read them and are themselves now uncalled, so the check sees a reference and the two strings are invisible to it. A dead reader hides a dead key. Same shape as the `pc` alias gap above, and another Task 322 row.
- 0|533| Renaming an element chases every reference to it, closed 2026-08-28. The reported half was `incidentLinks`, which is keyed by NODE and holds LINK ids, so a link rename has to rewrite ids INSIDE two of its arrays rather than rekey it — `applyLinkRename()` did not, and rename-then-drag threw on a missing `linkEls` entry. **The half the task asked us to go looking for was worse:** a CONTROL names its link (and may name a node) TWICE, in the parsed record and in `raw`, the sentence the exporter writes — so a rename produced an `.inp` whose `[CONTROLS]` names a link its `[PIPES]` does not declare, **which EPANET rejects outright**. A corrupt export beats a throw the user can undo. `doc.profiles[].stops` name nodes the same way. The token is replaced IN PLACE in `raw` (split on `(\s+)` keeps the whitespace runs), because that text is the user's own — their spacing, their capitalisation. The four references that were already right are asserted too, so a later unification cannot quietly drop one. `dev/lpn-spike/rename-references-harness.js` (21). Two fixture lessons recorded there: `addNode()`/`addLink()` MINT their ids and take none, so a fixture that names them is right for the first network in a process and wrong for the second; and `lpnExportInp()` refuses with `detail: length` when the unit selections are empty, which is about units and not about pipes.
- 0|408| Leader angle snap shipped 2026-08-27, as a MAGNET and hand-rolled: Settings > Map appearance > "Snap leader lines to angle", Off/15°/30°/45°, Off by default. `EngCalcs.lpnGeom.snapLeaderOffset()` is the whole mechanism — nearest increment, captured within a QUARTER of the step on each side so half the circle snaps and half stays free, radius untouched; half the step would snap every angle in the circle, which is the constrained behaviour Tom did not ask for. No library: the repo's no-dependencies-shipped norm against one modulo and one comparison, and `js/looped-network.js` does every drag by hand so there was nothing to plug one into. All three label drags (`nodelbl`, `linklbl`, an attached `label`) store through it; an unattached Text has no leader and is not snapped. Changing the setting re-snaps nothing already drawn. Guards: `dev/lpn-spike/geom-harness.js` (the magnet, incl. that most of the circle stays free) and `dev/lpn-spike/leader-angle-harness.js` (that all three branches are wired).
- 0|477| The New-project box shipped 2026-08-27, replacing the four-row File > New fly-out (and Task 437's standalone search, closed into this one). Four questions in one box: xy or lat/lon; a place-name field enabled only for lat/lon; every unit selector, with the SI and US presets as the fast path; and the head-loss formula, which the fly-out could never ask and which decides what the roughness column of every pipe means. **The fly-out's rows were the CROSS of two questions**, so a third would have made eight rows -- that, not row count, is why it had to go. Tom's argument against a wizard ("the fly-out ASKS FOR NOTHING") is answered by defaults rather than dismissed: every control opens on a working answer, Create is focused, Enter presses it and Escape cancels. Nothing in the box is a second copy -- the unit selects are CLONED from the page's own strip with the `name` attribute stripped (a clone that kept it would put the reinterpret-or-convert dialog in front of a project that does not exist yet), the presets read `EngCalcs.unitSets`, and the place text goes to `EngCalcs.lpnSearchRun()`, factored out of `lpnSearchOpen()` so the consent gate, the one-a-second rule and the chooser stay in js/lpn-search.js. `newBoxAnswers()` reads the box and `createProjectFrom()` acts on the value, which is what makes the ordering (units, method, baseline, tab, search) testable headlessly: `dev/lpn-spike/example-network-harness.js`. Four lang keys retired with the fly-out.
- 0|546| The lock banner pushed the map off the bottom of the screen, and could not be put away (Tom, 2026-08-27: *"There is a file save message that I believe is a culprit... If I reload the page, the message is gone and the map sizes correctly."* and *"I also don't like that this message is permanent and undismissable."*). Two faults in one banner. **The layout half**: it is chrome in FLOW above the canvas and arrived after the canvas had been sized, so the page sat one banner lower with a map sized for a page without one — the footer, both legends and the tile credit off the bottom. It now re-measures when its own height CHANGED, the same three lines renderTabs() uses for the tab strip wrapping, and it is the third piece of flow chrome allowed to do so by `dev/lpn-spike/map-height-harness.js`. **The dismissal half**: every warning banner is dismissable now, and the dismissal is remembered for ONE project and ONE error code, so a different fault, a different project, or locking recovering each bring it back on their own. A save that cannot check the lock while the warning is dismissed says it once as an expiring notice rather than restoring the banner — the message has one source, `lockUnavailableMessage()`, so the two cannot drift. Guard: `dev/lpn-spike/lock-banner-harness.js`.
- 0|549| The Find box, compacted for a phone, on Tom's four asks of 2026-08-27 (*"Maybe we could compact the find box somehow for phone..."*): Property and Condition share one flex line (`.lpn-find-pair`, with the `min-width: 0` that lets a long condition ellipsize instead of widening the box); the title "Find and replace" moved out of the body into the 40px drag band, reusing `.lpn-setbox-title` and its `pointer-events: none` so the band still drags; `lpn_replace_scope` became a `?` on the Replace heading, appearing exactly when the sentence would have and going away once a kind of asset is chosen. **The scroll fault was not the scrollbar.** The body always scrolled; the box was fitted to the VIEWPORT and then placed below `chromeFloor()`, so it hung off the bottom by the height of the chrome and the last results were below the fold rather than below a scrollbar. `capPanelToRoomBelow()` caps a panel to the room under the top it is actually given; the two Find branches that place without an anchor use it. Guards: `dev/lpn-spike/replace-harness.js` (the tip), `dev/lpn-spike/small-screen-harness.js` §12 (the rest).
- 0|550| On a touch device a geographic project announced itself as a grid. The coordinate readout was written by `pointermove` and nothing else, so with no hover it sat at its markup placeholder `X: --  Y: --` for as long as nobody dragged — visible in screenshot 0047, a phone on satellite imagery reading X and Y. That is the vocabulary rule, not a cosmetic one: `coord_order_check.php` blocks a commit over lat-before-lon in our own source, and a public picture of our own page saying X and Y over a street map argues the other way where strangers look. `refreshCoordsReadout()` writes the placeholder from the project, and ONLY when the kind changed, so a refresh inside one project never blanks a position a pointer just put there. `dev/lpn-spike/geo-project-harness.js`.
- 0|510| Saved paths, and the caret that opens them. Closed 2026-08-27 with nothing left: Tom, on reading the block, *"This seems simple. It should not be blocked on anything from me, and I find nothing for me to do."* `doc.profiles = [{id, name, stops}]` in the project file, the undo snapshot and the dirty signature; **the stops the user chose, not the resolved route**, because freezing a node list would preserve a path through pipes since deleted. An unknown id is kept verbatim, marked in the menu and named in a notice, but dropped from the DISPLAY. Which path is selected is deliberately not stored — that is a fact about a reader, like pane height. The caret takes `.lpn-tab-caret`'s own numbers (Tom: *"too small and non-conforming to be discoverable"*), grows SIDEWAYS only to 2.75rem below 40rem, and never routes through `openPane()` when Profile is already showing, or reaching for a saved path would arm the chooser every time. `dev/lpn-spike/profile-saved-harness.js` (54), `small-screen-harness.js`.
- 0|468| Demand categories on a junction, closed 2026-08-27 when Tom reworded the last string. A junction's demands are an ordered list: **row 0 is the junction's own `_demand`/`demandPattern`/`demandCategory` and `extraDemands` is every row after it** — EPANET's own asymmetry — so a one-demand junction is byte-for-byte the object it always was. **The category is a `;comment`, not a fourth column**, which is the whole import bug: the old reader stripped comments before splitting. Base demand means the SUM in the label, the ramp and the Tables column. NO per-category override: an override is keyed by element and property NAME, and a category has only a POSITION, which moves when a row above it is deleted. The popup puts every demand in ONE table, row 0 included (Tom, of the version that had it as fields above: *"I think this is a mistake"*), with the tips on the column headings. A category is searchable on junctions — the one property a single element holds several of, so `contains` and `equal to` ask every row. ONE deliberate non-identity: the `[JUNCTIONS]` demand column of a junction that also has `[DEMANDS]` rows is not written back, because EPANET discards it. `dev/lpn-spike/demand-category-harness.js` against an authored fixture, import → resolve → export → re-import with 14 tokens byte-identical, and the vendored engine reading the same file the same way. `lpn_result_demand_tip` was reworded by Tom on close. **Not verified: no browser pass on the popup table at 640px.**
- 0|543| The menus stopped outgrowing their names, 2026-08-27, on Tom's own four-part decision: View became **Map**, the Insert asset group became the FIRST row of **Water** as a submenu, the background image moved from Insert to Map, and the **Insert menu was deleted**. The principle he relaxed to get there: *"Everything about Water modeling is under Water"* had one standing exception, which was that every water asset was inserted from a menu of its own. What forced it was the terrain-elevation row — *"this is under View when it really has something to do with mapping, but nothing to do with view."* Evidence gathered first and it did NOT decide it: EPANET files its own Dimensions and Backdrop under View and nobody finds that strange, and it has no terrain feature at all, so the precedent settles where map furniture lives and not where this row goes. `lpn_menu_view` was renamed to `lpn_menu_map` and its 26 translations DELETED rather than carried, because every one of them was the word "View" in its own language. `lpn_menu_insert` keeps its word and its translations; only the menu it named is gone. The mnemonic-letter idea for asset rows stays unbuilt and belongs to the data-entry-clerk seat. Guards: `small-screen-harness.js` (five menus, and every hidden toolbar command still reachable, now through the fly-out), `id-prefix-harness.js`, `toolbar-harness.js`, `backdrop-scale-harness.js`. **The Map menu wears a PUSH PIN** (Tom: *"a nice push pin icon for Map would be good"*), drawn front-on rather than at the conventional 45° because a tilted 2px stroke turns the needle into a smudge at 17px, with the needle an UNSTROKED triangle so it can come to a real point — a stroked one paints four units across and reads as a nail. The eye is untouched and still draws the street-map and satellite rows inside the menu.
- 0|551| The Find result rows stopped listing the links that meet at a node (Tom, 2026-08-27: *"The results say 'Connected: nnn, nnn, nnn', and I don't know what that means... I think that it arose from tunnel vision on the Connected links task. Even though it's useful, I think we should remove it because it feels out of place."*). It was EPANET's Adjacent Links pane folded into the row, and it was wrong twice: it did not say what was connected, and it answered a question the search had not asked in the one place a reader is scanning for what they searched FOR. `lpn_find_adjacent` was DELETED from all 27 lang files the next day, on his ruling (*"Remove the keys."*): it named a feature that is gone, so it was debt rather than lost content, and the wording is in git.
- 0|552| Two legends in the same COLUMN but different BANDS printed through each other — the labels legend at top-right and the colour keys at middle-right, which is screenshot 0028's unreadable corner (`H= Head` cut off mid-row, `Elevation` showing through the pressure swatches). Task 516 had closed by giving them different DEFAULT corners, and the 2026-08-27 dodge compared bands, so neither could see a collision between a top box and a centred one. `legendInsetFor()` now tests a real overlap on BOTH axes and iterates (bounded at six passes, the inset only growing), a middle-anchored legend dodges by becoming an ordinary top-anchored one at the height its own centring gave it — clearing the transform, or it would land half its height high — and a legend only moves if it still FITS afterwards, because pushing one off the bottom of the map trades a readable overlap for a legend nobody can see. `dev/lpn-spike/small-screen-harness.js` §11.
- 0|502| A Text object now follows a LINK as well as a node: `anchorLink` plus `anchorT`, a FRACTION of the link's own arc length, so the note holds its place proportionally when an end node moves, a vertex is dragged or a bend is added. The three rejected attachments (always the midpoint; the nearest point recomputed each draw; a distance along) are argued at textAnchorPoint() in js/looped-network.js -- the one place an anchor becomes a point, for both kinds. Deleting the link takes its Text with it, which is deleteNode()'s existing rule. EPANET's [LABELS] anchor is a node only, so an export writes the drawn point and REPORTS the difference. Task 247's meter handle is the same {link, t} and drags what this only ever sets. dev/lpn-spike/text-link-anchor-harness.js.
- 0|529| Both legends gained **Off** as a placement (Tom, 2026-08-25, from his phone: *"one of the legend placement options must be 'Off'... Especially the labels legend. With all the control we have given the user, the legend is of less value now compared to when we were distinguishing coloured numbers."*). In the same dropdown, not a checkbox beside it, because where the box goes and whether it goes anywhere are one decision. The six positions had been written out twice, once per legend; now one `legendPositionOptions()` serves both, so the next option cannot be added to only one of them.
- 0|528| REGRESSION (Tom, 2026-08-25: *"Menus have moved down and away from their buttons"*): the 2026-08-24 rule lifting a downward panel clear of the WHOLE chrome was written for the draggable boxes, whose top band carries the drag surface and close button -- and openPanelAtAnchor() is also the one entry point for the menu pull-downs, which have no band. The Water menu hung below the toolbar and tab strip with nothing joining it to the word that opened it. Lifted only when the panel carries a `.lpn-popover-x`, derived from the thing the rule protects rather than a list of ids. dev/lpn-spike/popover-place-harness.js.
- 0|525| The EPANET engine-difference notes were re-emitted on EVERY solve, so a solve could never dismiss one -- a solve is what created it (Tom, from his phone: *"a solve didn't dismiss it"*). They are facts about the engine, not the solve, they are 0.08% and 0.6%, and the same sentences are already in the engine checkbox's tooltip. Said once now, per project and per engine choice. dev/lpn-spike/engine-note-once-harness.js.
- 0|524| On a phone the EPANET minor-loss note read as "stuck open" (Tom, from his own phone): #lpn_status is an advisory overlay standing until the next solve, harmless in a desktop corner and a quarter of the canvas on a phone. #lpn_mode_hint now goes below 640px -- it is mouse-verb advice ("Double-click a pipe") on a device with no double-click, and Task 253's clean-map already proves the map survives without it. #lpn_status stays, because it carries refusals, but is bounded: 33svh with scroll, and max-width UP from the inline 60% to 92%, since a narrow box makes a long note taller. dev/lpn-spike/small-screen-harness.js.
- 0|521| The map status strip named the units the PAGE booted in, not the PROJECT's: Calculators.lib.js fills it from the rendered defaults before init() installs the project's units, and the boot path never goes through refreshAllFromDocument(). Wrong on the first load of every project in an SI-defaulting language. Found by Tom from a screenshot; the obvious check PASSED against the broken code and had to be rewritten to reload.
- 0|505| `~/webdev/` now holds the projects, by SYMLINK rather than by moving anything: `~/webdev/engcalcs/hawsedc.com` points at `/var/www/cnm/public_html/hawsedc`, and `~/webdev/librewaternet.org` is its own empty repo. A physical move would have taken `hawsedc.local` down — `/home/haws` is `drwxr-x---` and Apache cannot traverse it. Layout and the reason: `dev/hosting-layout.md` section 5.
- 0|517| The georeference settle scaled `metersPerUnit` on `mpd.lat`, holding the model's LATITUDE span — the same sentence as holding its screen box only while the display was unprojected. Since Task 145's Mercator frame the model changed size at every settle as it travelled north or south. Scaled on `mpd.lon`, the screen width is now EXACTLY 1 across a settle. A second site (`georefFirstTransform()`, 6.4% on first placement) and a third (`georefRotateHandleSrc()`, the handle standoff) carried the same pre-Mercator assumption and went with it. `dev/browser-pass/specs/place.js` asserts all of it as derivations.
- 0|511| The whole browser pass, run end to end for the first time in a long while, had drifted to 19 failures across 711 checks. Every one was read rather than re-baselined: the georef three were two stale-spec and one REAL defect (Task 517, `metersPerUnit` scaled on `mpd.lat`), the 90-degree aspect is now DERIVED live from the page's own `lpnGeorefMetersPerDegree` (k = N/M, applied twice) instead of a pre-Mercator cos²(lat), the touch-tip failure was a real defect (Task 518), and a spec that threw was hiding seven whole sections (Task 519). Task 520 was spun out of the review. 959/959 checks, 35/35 sections.
- 0|512| The amber override ring read as a stuck highlight to two independent users while working exactly as designed. It now says what it is in two places neither needing a click: an SVG `<title>` on the ring naming the scenario, and a sentence on the scenario readout when the count is above zero. Tom ruled the ring stays ON by default — *"Default on. It's a good thing. I just couldn't figure out what it was."*
- 0|518| Three transport buttons lost their tips to a `className =` assignment: `setIconLabel()` added `.ec-help` and the next line wiped it, so Step back / Play / Step forward were unreachable on touch. One token, `+=`. `dev/browser-pass/specs/toolbar.js` back to green.
- 0|519| A throwing spec truncated the whole browser pass and the output looked like a passing one: `run.js` caught outside the loop, so `time.js` throwing meant 7 of 35 specs never ran. Caught per spec now, and every run prints `N/35 sections completed` with a SHORT RUN marker.
- 0|1| Roadmap reorganized: grouped by theme, priorities differentiated so ties are intentional, descriptions…
- 0|2| PWA evangelism: "⬇ Install" button added to navbar (before Save field), hidden by default. Shown only when…
- 0|3| Layout overflow: Wrapped <table class="bare"> in <div style="overflow-x:auto"> in echoCalculatorForm(). On…
- 0|4| PWA on mobile: PNG icons (192×192, 512×512) generated and added to manifest.json. Apple meta tags…
- 0|5| Touch tooltips: Bootstrap Tooltip initialized on all [title][style*="cursor:help"] spans via…
- 0|6| Orifice calculator phase 1: Orifice.php created with circular/rectangular shape selector, unit-aware…
- 0|7| Translations (multi-lingual): Evaluated cost/value of having a languages system in the post-2025 (AI) age.…
- 0|8| Added CLAUDE.md architecture and developer guide. Added php -l pre-commit hook. Priority 1 security fixes:…
- 0|9| Extracted per-calculator JavaScript into separate files under js/calculators/. (commit 76d6255)
- 0|10| Bootstrap 5.3.2 migration and jQuery removal. All pages converted to Bootstrap 5 utility classes; $()…
- 0|11| SVG sketches: Added to Orifice Drain Time (WSE, wall, H₁, D annotations), Weir-Irregular (crest profile as…
- 0|12| Orifice Drain Time calculator: Orifice-Drain-Time.php with conic volume method. Inputs:…
- 0|13| Solver (y/d₀ given Q) for Manning Pipe Flow: bisection solver added to js/manning-pipe-flow.js. Bisects…
- 0|14| Language-demand logging: logLanguageSelection() added to Language.lib.php; called when a valid ?lang=XX…
- 0|15| Add Amharic, Urdu, Swahili, Hindi, Arabic translations — complete translation of all calculators in each…
- 0|16| Micro-Hydro Power calculator: Retitled from Penstock-Design.php to Micro-Hydro-Power.php and migrated…
- 0|17| Chinese language code: Renamed internal code cn→zh (ISO 639-1 standard). Added normalizeLang() to…
- 0|18| Language system audit: Fixed all lang file issues. Removed ~30 orphaned legacy keys. Added missing…
- 0|19| Language menu order: Corrected Language.Settings.php order to alphabetical by English name (EU/UN…
- 0|20| Love is spoken — corrected 8 translations: it, sr, bg, cs, bn, hi, id, ur were saying "we speak about…
- 0|21| Rework message of love: added "You are not ruining everything" as the third clause in all 22 languages.…
- 0|22| Add km (Khmer), my (Burmese/Myanmar), ps (Pashto), fa (Farsi/Persian), uk (Ukrainian) as new languages —…
- 0|23| Irrigation landing page (Irrigation.php): added to menu with divider. Links to Weir Flow Simple, Weir Flow…
- 0|24| About page (About.php): added to nav menu. Covers global humanitarian open source mission, GNU GPL v3…
- 0|25| Language quality — structural fixes: he, pt, hr, sr, ro, zh all raised to 0.85–0.9. he: fixed 6 English…
- 0|26| Translated cs_Ec_target (and the sibling cs_lining_area, same defect) into all 25 non-English languages:…
- 0|27| Audit existing translations for glossary compliance: built dev/scripts/glossary_compliance_audit.php,…
- 0|28| Drip/Sprinkler Application Rate calculator (Drip-Sprinkler.php): inputs are average and minimum emitter…
- 0|29| Irrigation: Canal-Seepage.php added (prefix cs_). Inflow-outflow method: Q_loss = Q_in − Q_out, conveyance…
- 0|30| Robinson Rock Chute: Rock-Chute.php implemented — Robinson, Rice & Kadavy (1998) D50 sizing equations,…
- 0|31| Contextual hover tips: all javascript:alert help links replaced with span hover tooltips across all 27…
- 0|32| Translation sprint — three pages: Drip-Sprinkler.php (ds_* keys), Irrigation.php (body prose and card…
- 0|33| Drip-Sprinkler: DU quality check renders as "Good &mdash; DU &ge; 80% ✓" — fixed. HTML entities in ds_du_*…
- 0|34| Language button: replaced translated "Language" text with a globe emoji (🌐) — universally recognized, no…
- 0|35| Redundant phrases: Evaluated and closed. The only truly identical long passage across all 27 lang files is…
- 0|36| Text-only mode: Evaluated and closed. The PWA pre-caches all assets on install, making text-only redundant…
- 0|37| Progressive Web App (PWA): Implemented. manifest.json, sw.js, and icons/icon.svg added. Service worker…
- 0|38| Canal seepage expansion: Canal-Seepage.php expanded with lining payback outputs (annual value…
- 0|39| Save/share named calculations: URL-based Option B implemented. "Label:" field (50 chars,…
- 0|40| Lang-file syntax validator: Implemented scripts/lang_syntax_validate.php. Runs php -l per lang file and…
- 0|41| Lang-key parity checker: Implemented scripts/lang_parity_check.php. Compares each lib/lang.ec.??.php…
- 0|42| Translation payload generator (per-lang JSON): scripts/generate_translation_payloads.php now reads English…
- 0|43| Engineering glossary integration: scripts/glossary.json is now wired into both…
- 0|44| Zero-API translation runner (default): Added scripts/translate_zero_api.php to orchestrate default non-API…
- 0|45| Translation completion matrix: Added scripts/translation_completion_matrix.php to report untranslated-key…
- 0|46| New-calculator scaffold script: Added scripts/new_calculator_scaffold.php. Given --prefix and --keys, it…
- 0|47| Standalone engcalcs: Decoupled engcalcs from hawsedc.com via optional parent hooks.…
- 0|48| Encoding — kinematic viscosity tooltip raw codes: &sup6; is not a valid HTML5 named entity; it displayed…
- 0|49| "More" dropdown: About link moved under a "More ▾" dropdown (menu_more key, translated into all 27…
- 0|50| Math/logic review of all 14 calculators: Full review completed; findings written to dev/ai-report.md. One…
- 0|51| ec_lang_intent guard: $ec_lang_intent is now explicitly off-limits to AI in both CLAUDE.md and…
- 0|52| Add velocity checks to Manning trapezoid and irregular calculators: Added v_check result to both…
- 0|53| Propagate corrected rc_notes_4_def link to all translations: Replaced the old DOI URL with…
- 0|54| Hard-coded velocity units in Micro-Hydro messages/footnote: Updated velocity check output to unitless…
- 0|55| HTML-entity audit script + bulk fix: dev/scripts/html_entity_audit.php scans all lang files for HTML…
- 0|56| Audit remaining English strings in other languages: Parity checker run across all 26 non-English lang…
- 0|57| ec_lang_intent workflow audit and Spanish Robinson fix: Spanish Robinson translations verified correct —…
- 0|58| Wire glossary into CLAUDE.md agent translation sprint: Translation Sprints section updated with pre-sprint…
- 0|59| Velocity check messages shortened to OK/High !/Low !: Added EngCalcs.writeVelocityCheck() shared helper in…
- 0|60| Orifice submergence criterion fixed to use centroid, not invert: js/orifice.js submerged flag compared TWE…
- 0|61| Manning Pipe Head Loss HGL₂ NaN fix: js/manning-pipe-head-loss.js had hgl2 = hgl2 - hv (self-reference…
- 0|62| Velocity-check short labels use "High ⚠" / "Low ⚠" (icon carries the warning), not "High !" / "Low !" —…
- 0|63| CSS standardization for validity/status checks: Added named classes to engcalcs.css…
- 0|64| Translation sprint — velocity-check short labels + orifice centroid reword: Launched 26 parallel haiku…
- 0|65| Velocity checks added to Darcy-Weisbach, Hazen-Williams, Manning Pipe Flow, Manning Pipe Head Loss: All…
- 0|66| Velocity-tip wording upgrade (open-channel + enclosed-pipe): Per user feedback, richer tooltip wording for…
- 0|67| Removed $ec_lang['ec_name_invalid'] (and its empty $ec_lang_intent entry, English-only, removed with…
- 0|68| Drip-Sprinkler.php simplified — removed Distribution Uniformity (DU): DU as implemented required both an…
- 0|69| Expand and tighten glossary.json: Filled in all 5 empty languages (am, bn, km, my, ps) for all 27 terms…
- 0|70| Finish the tooltip-icon CSS standardization: the earlier "CSS standardization for validity/status checks"…
- 0|71| Removed orphaned rrc_main_desc and rrc_main_menu keys from all 26 non-English lang files: legacy of an…
- 0|72| Fixed HTML-entity-vs-literal-character blind spot in lang_parity_check.php and…
- 0|73| Translated the 3 keys newly surfaced by the entity-normalization fix (see next item): cs_payback_years in…
- 0|74| Lang-file key-order normalizer: Added dev/scripts/lang_key_order_normalizer.php, which rewrites each…
- 0|75| Deployment workflow script: Added dev/scripts/deploy.sh wrapping the full release sequence — php -l on…
- 0|76| Quality-score updater: Added dev/scripts/update_quality_score.php (usage: php update_quality_score.php…
- 0|77| [
- 0|78| Irrigation Pressure H-vs-P decision, resolved 2026-07-04, corrected same day: initial pass kept…
- 0|79| "Default values" reset button, added 2026-07-04: placed on the same shared row as the unit-set buttons…
- 0|80| Results sharing made opt-in, 2026-07-05: implemented the scope agreed 2026-07-04 (see prior framing above,…
- 0|81| Fixed bg/es/pt/tr Manning Trapezoidal Channel (mtc_) symbol/translation gaps found 2026-07-05: added the…
- 0|82| Suite-wide symbol-convention question, resolved 2026-07-05 (split off 2026-07-04 from the Irrigation…
- 0|83| npm/Composer dependency-management task closed as stale, 2026-07-05: investigated before starting (item…
- 0|84| Renamed irr_main_menu from "Irrigation Flow Measurement" to plain "Irrigation" in all 27…
- 0|85| TypeScript migration item closed as stale, 2026-07-05 (Human authorization): item was conditional on its…
- 0|86| [CC] **DONE 2026-07-07: Task 86.** Reversed the dw_roughness over-consolidation. dw_roughness restored to…
- 0|87| [
- 0|88| [
- 0|89| [
- 0|90| [
- 0|91| [
- 0|92| [
- 0|93| [
- 0|94| [
- 0|95| [
- 0|96| [
- 0|97| [
- 0|98| [CC] **[DONE 2026-07-13] English-improvement pass, 7 items.** The durable output is the Simple-English…
- 0|99| [DONE 2026-07-13] Removed the broken mph option from Manning-Irregular.php's velocity unit select
- 0|101| [
- 0|102| [
- 0|103| [
- 0|104| [
- 0|105| [
- 0|108| [DONE 2026-07-14] Install.php localization
- 0|109| [
- 0|119| [
- 0|120| [
- 0|121| [
- 0|122| [
- 0|123| [
- 0|124| [
- 0|125| [
- 0|126| [
- 0|127| mhp_diameter tooltip restored suite-wide
- 0|128| [CLOSED 2026-07-21] Trap-term native-confirmation residue (sw specific gravity + my/he head)
- 0|129| [
- 0|130| [
- 0|131| [
- 0|132| [
- 0|133| [
- 0|134| [
- 0|136| [
- 0|137| [
- 0|138| [
- 0|139| [
- 0|140| [
- 0|141| [CLOSED 2026-07-27 as already answered] Is Kichwa (sw) / الرأس (ar) really the hydraulic-head term?
- 0|142| [DONE 2026-07-28] ip_max_head label vs. tip mismatch — resolved on PRESSURE
- 0|143| [
- 0|145| [DONE 2026-08-24] Geographic projects: grid-or-geographic declared at creation, the OpenStreetMap raster basemap, the placement tool (File > Convert to lat/lon…), and the PROJECTION SEAM — a geographic document is DRAWN in Web Mercator and STORED in lon/lat, the whole boundary being outwardY()/inwardY(). Scope and the rejected alternative: dev/geographic-projects.md; the placement tool: dev/georeferencing.md. The boot-time declaration went to Task 477 and the float32 rebase to Task 439.
- 0|146| [
- 0|146.01| [CC] **[DONE 2026-07-30] Draggable data labels on leaders, collision avoidance, background mask (lpn_).**…
- 0|146.02| [DONE 2026-08-08] EPANET-style icon toolbar — shipped as Task 231; map symbols extracted to 146.10
- 0|146.03| [
- 0|146.04| obsolete 2026-08-21. Node/link report tables shipped as the bottom pane's tabs (Task 434).
- 0|146.05| Deprecated 2026-08-22 by the asset tables, which shipped as the bottom pane's tabs (Task 434). Tom: "We have the asset tables now. I think that deprecates this task." One live consequence carried forward as an open question, not a decision: the Text row was removed from the Settings panel's ID-prefixes list on 2026-07-30 because a text element's ID was unreachable from every screen, and `settings.idPrefixes.T` and `nextId.T` were both kept — so restoring it is one array entry in `rebuildSettingsFields()`, worth doing IF the asset tables now show a Text ID.
- 0|146.06| [
- 0|146.07| [
- 0|146.08| [CC] **[DONE] Multiple named saved networks (lpn_).** Local multi-project save/retrieve, shipping the…
- 0|146.10| [
- 0|147| [
- 0|148| [DONE 2026-07-27] template_welcome's >> ... << markers replaced with CSS italics
- 0|149| [
- 0|150| [
- 0|151| [
- 0|152| [
- 0|153| [
- 0|154| [
- 0|156| [DONE 2026-07-28] .git and directory listings were readable over HTTP
- 0|157| [DONE 2026-07-28] index.php now has a real meta description
- 0|159| [
- 0|160| Turkish accusative on the app name settled as EngCalcs'ı (back unrounded final vowel); 3 keys fixed
- 0|161| [
- 0|162| [
- 0|163| [
- 0|164| [
- 0|165| [
- 0|166| [
- 0|167| [
- 0|168| [
- 0|169| [
- 0|170| [
- 0|173| [DONE — built during Task 146, closed 2026-08-03] EngCalcs.initTips(root) — tooltips built after page load…
- 0|174| [
- 0|176| [
- 0|177| [
- 0|179| [CC] **[DONE 2026-07-30] Tom's second lpn_ review round.** Five pieces of test feedback, three of which…
- 0|180| [
- 0|182| [
- 0|183| [
- 0|184| Project/scenario model: the DELTA model — CLOSED 2026-08-14
- 0|187| [
- 0|188| [
- 0|189| [
- 0|190| [
- 0|193| [CC] **[DONE 2026-07-31] lpn_ English tightening pass.** The English-reform gate run before the 146.06…
- 0|195| [
- 0|196| [DONE 2026-08-11] EPANET .inp IMPORT (Task 146 child). Export is NOT built — Task 281
- 0|197| [
- 0|198| [
- 0|199| [
- 0|200| [
- 0|201| Scenario UI — closed 2026-08-17 as already done, found in the same audit. `setOverride()`/`clearOverride()`, create/rename/delete/switch, the override-count readout in the status strip and the per-row "Only in this scenario" tick all exist and are exercised by `dev/lpn-spike/text-scenario-harness.js`. One bullet was NOT built and was superseded rather than dropped: scenarios live in a MENU, not in a bottom tab strip mirroring project tabs (Task 211's reserved space).
- 0|203| [ADOPTED 2026-08-05] The COVERAGE MATRIX: a core cross of calculator × language
- 0|204| [
- 0|205| [
- 0|206| [
- 0|208| [OBSOLETE 2026-08-05] A lock that travels with a COPY of a file is the wrong lock
- 0|209| [DECLINED 2026-08-13] A snoozable tip system (originated during Task 146)
- 0|210| [
- 0|211| [
- 0|212| [
- 0|213| [
- 0|214| Glossary anchor languages realigned to es, pt, fr, tr; declared in glossary.json meta.anchor_languages
- 0|215| [
- 0|216| [
- 0|219| [
- 0|220| [
- 0|222| Positioning against epanet-js: the thinking lives in `dev/positioning.md` and the content residual is discharged. Tom ruled 2026-08-17 that About.php has no good place for the engine claim — "It's already in our page titles" — so it is made once, in his own words, as the examples-gallery welcome line (`lpn_examples_welcome`): "Welcome to your Water Supply Network software, with the EPANET core". A qualification, not a headline; `about_body_html` was left alone and its 26 translations with it.
- 0|223| [
- 0|224| [
- 0|225| [
- 0|226| [
- 0|227| [DONE 2026-08-08] prod_smoke.php --links now follows the links our pages emit
- 0|228| A share affordance at the naming moment, 2026-08-18. "Copy link to this calculation" sits under the Printable Subtitle input in `echoCalculatorForm()`, so every calculator gets one implementation. **No silent path:** a missing `navigator.clipboard`, a missing `writeText`, a rejected promise and a synchronous throw all reveal a focused, selected url box saying "Copy this link:". Measured through the EXISTING `logSignal()` writer as a sixth event, `share`, with `detail` = `copy` or `manual` — so the row says both that it was used and how often the clipboard path is unavailable in the field. **No new storage**, so `consent_body` stays true and `EC_CONSENT_VERSION` is untouched. What is NOT measured, and cannot be without new storage: whether anyone ever OPENED a shared link — a shared URL arrives as an ordinary page view. `log/lang-log-stats.sh` prints it against the `title` rows as an order of magnitude, not a rate. No footer "tell a colleague" line was added; that was the design this replaced. Harness `dev/calc-spike/share-link-harness.js` (18).
- 0|229| [DONE 2026-08-08] The drift tripwire can tell "no translator needed" from "nothing changed" —…
- 0|230| [
- 0|231| [
- 0|232| [DONE 2026-08-08] Irrigation.php removed — the page and its 17 keys are gone
- 0|233| Manning-Irregular opened in metric on English pages, with a ⚠
- 0|235| [
- 0|236| [
- 0|237| [
- 0|238| [
- 0|240| [
- 0|243| [
- 0|244| Navbar carries the distinguishing term: a GitHub mark plus "Libre Software" (`menu_libre`, `#nav-libre`), linking to the README license section rather than a page of ours. "Forever" stayed out of nav copy.
- 0|245| [
- 0|246| lpn documents are `.lwn` outside and JSON inside — Tom bought LibreWaterNet.org and named `lwn` the stable name. Save writes `.lwn` only; Open still accepts `.json`, including the old `-lpn` and `-lpn-hawsedc-engcalcs` name suffixes, forever. New files carry NO `-lpn` suffix: it existed only because a generic `.json` could not say what the file was, and `.lwn` says it where an OS looks. Open/Save/Save-as use the existing `lib/Icons.lib.php` objects (folder, floppy) rather than new drawings; **New was deliberately left off the toolbar**, because Tom removed it by name on 2026-08-15 — the later, more specific instruction beat this task's older wording. `file-naming-harness.js`, 42 checks.
- 0|248.01| Time settings, editable, 2026-08-18. The seven `[TIMES]` values (duration, hydraulic step, pattern step, pattern start, report step, report start, start clock time) are a form in a new **Time** tab of the bottom pane, written through `EngCalcs.lpnTimeSetField` — which stores the seconds AND the text the user typed, so `24:00` never silently becomes `86400` and a rejected edit changes nothing at all. A bare number is HOURS, EPANET's rule. `js/lpn-time.js`; harness `dev/lpn-spike/time-harness.js`.
- 0|248.02| Patterns on a RESERVOIR HEAD and on a PUMP, 2026-08-24. The property popup attaches one to each, beside the junction demand pattern it already had; the head is multiplied (EPANET's Head x Pattern) and the pump's curve is scaled by the affinity laws, H = s^2 ( h0 - a (Q/s)^b ). **A pump's speed pattern REPLACES its speed setting rather than scaling it** — measured against the engine, not inferred, and the exponent 2-b (not -b) is 87 ft of pump head at s = 1.32. `[RESERVOIRS]` column 3 and `[PUMPS] SPEED`/`PATTERN` round-trip byte-identically; harness `dev/lpn-spike/pattern-attach-harness.js` against `reference/pattern-attach.inp`.
- 0|249| [
- 0|250| [H] **[DONE 2026-08-13] Where do we explain lpn at all? A Help menu on the page.** openHelpMenu():…
- 0|251| [
- 0|252| [
- 0|253| Clean map shipped 2026-08-18: one toggle, on the View menu and in the toolbar's View group behind a camera icon, that hides `#lpn_mode_hint` and `#lpn_coords` and nothing else. Not stored and not part of the project — it is a presentation mode you hold while taking pictures, and persisting it would let a user lose the mode line permanently with nothing to blame. `display:none` rather than `visibility:hidden` so `overlayReserve()` measures zero and `zoomExtent()` gets the strip back. The units/scenario readouts stay: a screenshot of bare numbers that does not say what they are is worse than one with a coordinate tracker in it. Task 175 still holds the printable-version question.
- 0|254| [
- 0|255| [DONE 2026-08-09] lpn_ was solving US networks with the length in the wrong unit; head loss was 3.281x too…
- 0|256| [DONE 2026-08-09] dev/lpn-spike/popup-tips-harness.js was dead
- 0|257| Example projects chosen and shipped: `examples/` holds Net1, Net2, Net3, Elm Street Center and the Basic US/SI networks, each with an `.svg` thumbnail. Closed on Tom's ruling 2026-08-17.
- 0|258| [
- 0|259| [
- 0|260| [
- 0|261| [
- 0|262| [
- 0|263| [
- 0|264| [
- 0|265| [REVERTED 2026-08-10, same day it shipped] Units do NOT go on the browser tab
- 0|268| [
- 0|270| [
- 0|271| [
- 0|273| [
- 0|274| [
- 0|275| [DONE 2026-08-10] The Settings panel says "Saved with this project."
- 0|276| [
- 0|277| [
- 0|281| EPANET `.inp` EXPORT shipped 2026-08-18. `EngCalcs.lpnExportInp()` lives in `js/lpn-inp.js` beside the reader (one opinion about the format), writes the PROJECT's units, and emits `[COORDINATES]`/`[VERTICES]`/`[LABELS]` with a label's point shifted back to the upper-left corner EPANET means. Acceptance met: 1,280 numeric tokens across Net1/Net2/Net3 come back character-for-character, verified a second time by an independent comparison. Five round trips are genuinely impossible and are reported rather than faked — a pump with no curve, a multi-line Text label (flattened; `[LABELS]` is one quoted string per row), curve-point TEXT, an emitter coefficient, and a backdrop image's file name. File > Export EPANET file… is the way in. Harness `dev/lpn-spike/inp-export-harness.js`, mutation-tested against 7 mutants.
- 0|286| EU ePrivacy work complete: the ten `consent_*`/`privacy_link`/`terms_link` keys are translated in all 26 languages (verified 2026-08-17, including the 0.65 tier). Rules live in CLAUDE.md; the record is `dev/cookie-storage-inventory.md`. Task 287 already closed.
- 0|287| [
- 0|288| [DONE 2026-08-12] The unique identifier is gone. What is stored is one digit per page
- 0|289| [
- 0|290| [
- 0|291| [
- 0|292| [
- 0|293| [DONE 2026-08-13] Extract the pure functions out of js/looped-network.js so the map editor becomes testable
- 0|295| [
- 0|296| [
- 0|297| [
- 0|298| The navbar's "More" became **Help** and moved beside the Language picker, 2026-08-18 — `menu_more`'s VALUE changed (a resync, not a rename; `detect_english_drift.php` now lists it), the `<li>` moved verbatim into the right-hand `ms-auto` list ahead of the language dropdown, id `dropdown-more` → `dropdown-help`, and it took the existing `help` icon, so no icon and no CSS changed. Verified across all 33 top-level pages via `render_page.php`, one page per process. **Two things in the old block were stale:** Task 244 was already CLOSED (the FLOSS mark shipped 2026-08-09), so there was nothing to decide it against; and "Walkthroughs now joins them" was superseded the same day by Tom moving Walkthroughs OUT to the Looped Network page's own Help menu ("the walkthrough is a little incongruous"), which the code comment records and this followed. The navbar's zones, and why the right strip is now full at two items: `dev/navbar-strip.md`.
- 0|299| [
- 0|300| [DECLINED 2026-08-13] A "New project" wizard picking units AND friction method
- 0|301| [
- 0|302| [
- 0|304| The project file's NAME and EXTENSION — CLOSED 2026-08-14
- 0|305| How a visitor opens an EXAMPLE, and the New-vs-Open lie — CLOSED 2026-08-15 into Task 314
- 0|308| [
- 0|309| [
- 0|310| [DONE 2026-08-13] Compare-Languages.php no longer fatals on a bare visit
- 0|311| [
- 0|312| [
- 0|313| [
- 0|314| An EXAMPLES LIBRARY, on the HEC-RAS model — BUILT AND SHIPPED 2026-08-14
- 0|315| The 30-character filename — CLOSED 2026-08-14
- 0|317| Push Base values PER ELEMENT — CLOSED 2026-08-14
- 0|318| [
- 0|319| Accept-Language log injection, in five copy-pasted writers — CLOSED
- 0|320| Moved ## Completed into the archive: ROADMAP 7,067 → 3,325 lines
- 0|321| formmail.php read five $_POST keys with no isset()
- 0|323| Scenario writes that bypassed setProp
- 0|324| [
- 0|325| Symbol size and text size ARE independent now — closed 2026-08-17 as already done, found by Tom asking why it was still open. `symbolFactor()` is `settings.symbolSize / 2 / JUNCTION_R / state.s` with no `textFactor()` in it, `settings.symbolScale` (the old "relative to text" multiplier) is deleted on load by `migrateSaved()`, and `linkWidth` is its own screen-pixel setting. The block was a stale transcript of the pre-change state. Unbuilt ideas it carried, if ever wanted: label-background masking toggle, and whether map/screen units and size are really one setting.
- 0|326| The paper-units paradigm — CLOSED 2026-08-14 as ALREADY DELIVERED, and it was never a build
- 0|327| The thematic view got its one-click control 2026-08-18: a single select on the toolbar naming the field, no panel to open. ONE control for both node and link fields — choosing one clears the other, because two quantities coloured at once is a map with two legends and no way to tell which is which. Pressure and velocity lead the list, the rest follow EPANET's own View-menu order. The Settings rows still exist and the two are kept in step in both directions. Browser spec `dev/browser-pass/specs/color.js`.
- 0|328| The leader stored the TEXT's corner, so the angle slid with the zoom — CLOSED 2026-08-14
- 0|329| Pipe labels aligned ALONG the pipe, GIS-style — SHIPPED ON 2026-08-15
- 0|330| Toggle for label background masking
- 0|331| GIS paradigm phase 1: text/symbol/pipe sizes are three independent SCREEN-PIXEL settings, and labels hide…
- 0|332| Imported EPANET labels render at EPANET's own anchor, not converted
- 0|333| Label prefixes, suffixes and one blanket separator — SHIPPED 2026-08-15, and the label COLOURS are gone…
- 0|334| One .lpn-annotation class, declared where the element is built
- 0|335| Store a dragged label's offset in screen pixels — SUPERSEDED by Task 328, not shipped
- 0|336| Link label values on ONE LINE — SHIPPED 2026-08-15 with Task 333's second round
- 0|337| Text label Bold and Rotation, with Match-pipe and Flip buttons. Rotation is a stored number, not a link to the pipe (Tom's call)
- 0|338| GEOMETRY IS NOT SCENARIO STATE — confirmed correct and deliberate, never built. Dragging a node inside a scenario edits Base because `x`/`y` are not in `LPN_OVERRIDABLE`: two scenarios of one network must overlay identically or a side-by-side reading is impossible. Its only remaining work was to say so in the UI; Tom ruled that ineffective 2026-08-17 and deleted it, routing discoverability to the Walkthrough (see Task 412). The reasoning lives at `LPN_OVERRIDABLE` in `js/looped-network.js`.
- 0|340| A Text label hides at a threshold scaled by ITS OWN size
- 0|341| A pipe too short to carry its label does not carry one — CLOSED 2026-08-14
- 0|342| MTEXT rung 1 shipped 2026-08-17: a Text object's `_text` may hold newlines, edited in a textarea, one tspan per line with the block straddling its point (`textLabelFirstDy()`), and one line renders pixel-identically to before. Justification rows (`lb.align`/`lb.valign`) landed with it, Base-owned. Rung 2 (a wrap WIDTH, the thing that actually makes it MTEXT) and rung 3 (per-run rich text, declined) are unbuilt; the EPANET `[LABELS]` one-string-per-line constraint is Task 281's to settle. Harness `dev/lpn-spike/text-multiline-harness.js`, mutation-tested against 4 mutants.
- 0|343| SUPERSEDED by Task 397. Its open question — what triggers a drop — was answered by Tom's 2026-08-16 phases: first-fit drops whoever arrives at a full space, so the trigger is the priority ORDER, never a threshold
- 0|344| The element property box is DRAGGABLE — SHIPPED 2026-08-15
- 0|345| "Apply to all" beside each ID prefix — SHIPPED 2026-08-15
- 0|346| REJECTED, twice — no extrema tie rule. A mark that vanishes once enough elements share it confuses users more than a shared mark. Do not re-propose.
- 0|349| A long pipe repeats its label along itself
- 0|350| "Always show" on a Text label
- 0|351| A readability-bias angle for aligned pipe labels
- 0|352| Recent files go LAST in the File menu
- 0|353| Find shipped 2026-08-17: Edit > Find opens a modeless panel with scope (all / each element type), property, condition (contains / is exactly / greater / less) and a value. It reads `colorValueOf()`, so a condition is on the number PRINTED on the map, in the displayed unit; results select and PAN, never zoom; a result row re-resolves its element by ID, so a stale row is inert. Read-only by construction — Task 389 is this query plus a `setProp()` write. Harness `dev/lpn-spike/find-harness.js`, mutation-tested against 5 mutants.
- 0|354| A pipe VANISHED past ~47x zoom on a state-plane model
- 0|356| A zoom step no longer rebuilds labels nobody can see
- 0|357| Zoom-to-fit stops padding for labels it is not drawing
- 0|358| The canvas can no longer grow taller than the window
- 0|359| Zoom to fit is a FIXED POINT now, so the same drawing fits the same way every time
- 0|360| The map remembers where you were looking
- 0|361| "Map size" always names its dimension, and the standard is min
- 0|362| CSS PIXELS ARE THE SIZING UNIT — decided 2026-08-15 after two rounds of argument, and recorded so it is…
- 0|363| A shorter map floor, and no validator badges under the canvas
- 0|364| A pan is not a zoom, so it no longer re-lays-out
- 0|365| The W3C validator badges are deleted
- 0|366| A zoom no longer measures or recomposes anything
- 0|367| The label mask was eating the pipes
- 0|368| One element-type order in all three places
- 0|369| The aligned-label flip was measured in the wrong FRAME
- 0|370| The toolbar carries Save and Save as; a fat-finger tap opens what is there
- 0|371| A Text label's mask became a white sheet over Net3
- 0|372| One shared popover placer: flips below/above, caps body height instead of clamping around, menubar clicks now dismiss
- 0|373| The dirty asterisk had two causes, neither an edit
- 0|374| Aligned label spacing is half the smaller map dimension
- 0|375| Six of the seven automatic zoom-to-fits are gone
- 0|376| Label mask rect replaced by a text halo (paint-order stroke fill); the mask element, its pad, its rotation and its layer are gone
- 0|377| REJECTED by Tom 2026-08-16: hiding a conflicting label is not its own task, it is the tail of Task 379
- 0|378| CLOSED 2026-08-23. `drawExampleNetwork()` is out of `js/looped-network.js` — 280 lines that no menu item reached, shipping to every visitor for the test suite's benefit; the file is 20,196 lines. Seven of TEN harnesses (the block said seven of seven) moved onto `dev/lpn-spike/example-fixture.js`, which opens the shipped gallery file through `acceptImportedText()` + `applySaved()` — the path a visitor takes, which the code path never was. The last three could not: each asserts something about the code-drawn drawing itself, about a new project's seven colour classes (the gallery file predates the per-group colour keys, so `applySaved()` pins it to five), or about the shipped `textSize` 11 against the gallery's 9. So the function was MOVED verbatim to `dev/lpn-spike/example-draw-fixture.js` and spliced back into the module's own scope by `loadLoopedNetwork()`'s new `preludeSource` argument — it still closes over the real `doc`, `settings`, `setProp()` and `el()`, so a renamed seam throws instead of passing quietly. Migrating the three instead would have meant re-fitting their assertions to a different network, which is changing what a test tests in order to land a refactor. Every migrated harness ran byte-identical to its pre-change stdout; a mutated fixture fails five assertions, which is what proved the relocated copy is the one running. Also found while migrating: the SI gallery example's pump carried `_diameter: 4`, a US inches value in a millimetre document. The `settings.engine` stub finding is Task 496.
- 0|379| Label relaxation replaced by candidate scoring: ranked goals, oriented boxes, a neighbourhood term, no cap and no failure condition
- 0|380| A dragged label stores its leader endpoint in MAP units, and that is CORRECT — closed 2026-08-15 as…
- 0|381| Pipes are obstacles now, at a low weight
- 0|382| A label you have just placed says so, then stops
- 0|383| Leaders are segments, weights mean insistence, the debug mark is screen pixels
- 0|384| Colour by value: EPANET's four-break/five-band legend, per-variable absolute breaks, Equal intervals/Equal counts, three ramps, a thematic mode. epanetjs is FSL-1.1-MIT, so the idea only
- 0|385| Giant obstacle boxes were a stale measurement, not rotated-box waste
- 0|386| Repeated-label spacing was really under min/2
- 0|387| `dev/browser-pass` silently tested the WRONG TREE from a worktree (constant port 8899, docroot `REPO/..`). Fixed 2026-08-17: OS-assigned port, docroot from `git rev-parse --show-toplevel` via a mkdtemp symlink, and a random per-run sentinel fetched back before the browser launches — the readiness probe is the sentinel, not the page. `mi-defaults.js`'s private copies deleted. The stale specs it exposed are Task 414; the general lessons are in `dev/testing-notes.md`.
- 0|388| Closed 2026-08-23 with the last item done. The roadmap half (11,871 lines -> 3,265, then a 321-line ID ledger), CLAUDE.md (1,213 lines -> 560, 36%) and the commit-message rule all landed 2026-08-16; `js/looped-network.js` was held back because a concurrent track owned it. **The 47% figure was stale as a measurement and that was the finding:** it measured 47.4% at `1d10aac2`, then the file grew 4,832 lines of which only 1,220 were comments, so the ratio drifted to 42% on its own — inside the band its already-compacted siblings occupy (lpn-solver 36%, lpn-inp 41%, lpn-collide 43%, lpn-geom 45%, lpn-time and lpn-epanet 48%). So the honest line reduction was 30, and the real yield was SUBSTITUTION: **16 stale or false claims deleted**, including "nothing sets 'active' yet" (four places do), "this page is Hazen-Williams only" (a method selector shipped with Task 271), four pointers to a Visibility panel Task 441 dissolved, a privacy claim CLAUDE.md bans (place-name search "is not built here on purpose" — `js/lpn-search.js` shipped as Task 437), an elapsed-time fabrication, and a harness count that had drifted from ten to "seven", now replaced by its own derivation so it cannot drift again. Diff discipline verified: zero changed lines that are not comment lines. The DW-roughness label defect it found is Task 491; the commit-message-rewrite question is parked as Task 492.
- 0|389| Shipped 2026-08-24 as a section of the Find panel, not a second dialog: Task 353's selector decides WHICH elements, and the write below it decides what one property becomes. **The writable properties are `pushSpecList()`'s** — the list the Settings push and the scenario push already share — so nothing invented a fourth opinion about which properties are inputs, and the scope falls out of it honestly: diameter, roughness, minor loss, demand and elevation. Results are searchable and not writable; `length` is left out with them because `lenAuto` ("follow the drawing") is a second decision per link that one value box cannot express, and ID/Text are out because an id is an identity, not a value. **The count is the confirmation** — it counts what will CHANGE, not what matched, so a pipe already at the new value is excluded — and Cancel is safe by construction because the preview only measures. One `saveUndoSnapshot()` for the whole set, so a 37-pipe replace is one Ctrl+Z rather than 37 against an `UNDO_LIMIT` of 20. Every overridable write goes through `setProp()`; elevation is the deliberate exception, Base-owned survey data with no `prop` in the spec list. Guarded by `dev/lpn-spike/replace-harness.js`, mutation-tested five ways — and the setProp mutant is the one to note: `scenario_seam_check.php` PASSES a computed write (`el['_' + spec.prop]`), because it matches literal property names, so the guard and the harness cover different halves of the same rule.
- 0|392| Label candidate reach was 28 px, smaller than the label itself. Now 30 text heights with the inner ring at 6, and the number is a multiple of the lettering rather than an absolute
- 0|393| WITHDRAWN 2026-08-16 the same day it was raised: the "residual" was my own harness's parser, not the solver. With demands right, Net3 matches EPA's report to 0.01 ft and 0.3 gpm
- 0|397| Phase 0: the Labels-box priority column (one column, two axes, directions compiled in), and the local feature context — one per-node record of scale-invariant bearings plus the rounded model values the drop comparator reads, built at the one seam whose lifetime already matched
- 0|398| Phase 1: node labels by priority-ordered first-fit, two sides and a drop, replacing the ring for auto-placed nodes. Links place first but yield to a node that has nowhere else to go
- 0|399| Phase 2: a link label sheds values by the user's priority instead of vanishing whole — on its own pipe's length AND on conflict with another pipe's label. linkLabelTooShort() is now the cascade's last rung
- 0|401| flips under pan MEASURED at 0 on the example, Net2 and Net3 — the view-rect station cull can reach node placement in principle but does not move a label in practice. The ?debug=labels readout stays as a standing tripwire
- 0|402| NOT A DEFECT — the drifted Net2/Net3 numbers came from Tom editing already-corrupt local copies, not from the app. Corpus restored from git and verified: 1908 pass-through checks reproduce every number from EPANET's own .inp
- 0|403| The headless stub learned that text width follows FONT SIZE, plus DOM-honest `textContent` (assigning replaces children, reading includes them), `firstChild` seeing the text node, and a `removeChild` that clears it. The style OBJECT outranks the style attribute, which outranks a bare `font-size`. The real defect it exposed was fixed first: `drawExampleNetwork()`'s callouts now anchor by `lb.align` at the label's near EDGE, so the text width cancels and no stored offset can go stale. Harness `dev/lpn-spike/stub-fidelity-harness.js`.
- 0|404| Is the shed still too eager? NO — Tom, 2026-08-17, judging from production at commit `22db1f9`: at most a little more padding than necessary in some cases. The two 2026-08-17 causes fixed (font-size pad, link labels shedding for dropped node labels) stand as the result.
- 0|405| Content moved into Task 459 on 2026-08-22, when the three sprint tasks were consolidated into one. Four English strings resynced after sprint 397's zero-drift baseline: `lpn_settings_readability_bias`, `lpn_field_text_match_pipe`, `lpn_examples_size` and `lpn_ex_net3_desc`.
- 0|406| A stacked label is a STAIRCASE, not a rectangle: `Collide.labelLineBoxes()` cuts one collision box per line from the measured per-row widths (`noteRowWidths()`), and both passes score, test and RESERVE the staircase. The score takes the WORST row, never the sum, so a three-line label's overlap is not three times a one-line label's. A label with no `lines` is unchanged by construction. Harness `dev/lpn-spike/line-box-harness.js`, mutation-tested against 6 mutants. The `?debug=boxes` picture and the bench's own counts were still reading `r.box` when this first closed and had to be repaired the same day (Tom: "still shows one box, not a layer cake") — the geometry landing is not the feature landing. Browser spec `dev/browser-pass/specs/boxes.js` guards the drawing; nothing headless can.
- 0|407| Text elements gained scenario overrides, 2026-08-17: `text` and `active` only — position, size, anchor stay Base-owned, because a note that jumps around as you flip scenarios is unreadable. `active` is how a scenario gets "its own" note with no second mechanism (born in Base switched off). Text is a THIRD group: `elGroup()` classifies it by a positive `_text` test and `ovKeyFor()` gained a `'t:'` prefix, or a label id would collide with a junction id (Task 324). `migrateSaved()` renames `lb.text` → `lb._text` unconditionally, storage v7 → v8. Harness `dev/lpn-spike/text-scenario-harness.js`, mutation-tested against 7 mutants.
- 0|409| Profiles shipped 2026-08-18: View > Profile opens a modeless, draggable panel drawing ground, hydraulic grade and the pressure between them along a chosen path. Stations are LINK LENGTHS (`js/lpn-profile.js` cannot see a coordinate, which is the structural guarantee); the path is Dijkstra by length with click-to-add waypoints, not a path-editing language. **The vertical axis is TRUNCATED**: Net1 fills 79% of its frame against 30% anchored at zero, and the step is chosen by tick count rather than span/5 — that alternative floors Net3's 8–147 ft profile to −50 and wastes 58 ft of sky. It redraws off `applySolveResult()`, so it follows every edit with no Refresh button. Ground crosses into the result head unit (Task 422) so both curves share one axis honestly. Harness `dev/lpn-spike/profile-harness.js` (59) and browser spec `dev/browser-pass/specs/profile.js` (25), 12 mutants all caught. Left out: runner-up alternative routes, export/print, and time animation (Task 410). **CORRECTION 2026-08-18: epanet-js DOES truncate its axis** — Tom checked it directly, and the earlier note here that it was the thing they got wrong was mistaken. Truncation is still right; it is simply not a difference from them.
- 0|410| Shipped with extended-period simulation 2026-08-18, closed 2026-08-23 once somebody looked. `js/lpn-time.js` has all four things this task asked for — time display, play/pause on one `.lpn-transport-btn`, a speed picker and the frame slider — in the EPANET/HEC-RAS shape Tom named. Every frame is computed up front, so scrubbing is a redraw and not 25 round trips through WASM. It sat at priority 5 waiting on Task 248, which had already landed.
- 0|411| Most-open angle: four fixed corners as a rejection table, polar raster behind them, tolerance on the tester panel
- 0|412| A Base-wide property does NOT need to say so in the popup — Tom, 2026-08-17: "Absence of checkmark is enough." An unticked box on an overridable row already carries the meaning, and discoverability goes to the Walkthrough instead. A working `baseWideMarker()` was built and reverted the same session; it is in git at `33b5c0f` if ever reconsidered. This also discharged Task 338.
- 0|413| Dependency management adopted 2026-08-17 (Tom: "I am 100% on-board"). npm as a DEVELOPMENT-TIME acquisition and integrity tool only: root `package.json` with no `scripts` key at all, committed `package-lock.json`, `node_modules/` gitignored, nothing at runtime. A bundler is banned, not merely unneeded — `filemtime()` cache-busting, `git pull`-as-deploy and `sw_manifest_check.php` all assume the served file IS the source file. Composer out of scope (no PHP dependency exists); no PHP framework and no change of language. New `dev/vendor-manifest.json` + blocking `dev/scripts/vendor_integrity_check.php`, mutation-tested against 6 mutants. Policy: `dev/dependency-management.md`.
- 0|414| The dev/browser-pass/ specs run again against the real page: 139/139
- 0|415| A `selected` property on elements (view state only) and subject-then-verb Delete
- 0|418| The first project of a first visit is born clean. **Two causes, both signature-timing, and the first one alone was not enough.** (1) `init()` stamped `savedSig` before `seedDefaultInputs()` filled `settings.defaults`, which `docSignature()` covers; the baseline is now stamped after the seeding. (2) The real trap: `currentView()` returns **null** while `svg.clientWidth/clientHeight` are 0 -- the whole time before `applyMapHeight()` writes a height on `window load` -- so the baseline was stamped over a `view: null` document, and the first autosave after the canvas got a size saw a different signature. The transform never moved; only whether the view was REPORTABLE changed. Fixed by rebasing once in `noteMapSized()` after any deferred restore, and by `saveToStorage()` declining to judge dirtiness while `!mapSized`. `dev/lpn-spike/boot-clean-harness.js`, 13 checks (4 fail without it); the browser spec has still never been executed.
- 0|419| Suite adopted standard gravity 9.80665; minor-loss disagreement with EPANET now declared to the user
- 0|420| Goto element by ID shipped 2026-08-17 as part of the Find panel below — EPANET's Map Finder, including its Adjacent Links, said as one line under the found node. A single hit goes straight there rather than offering a list of one.
- 0|421| **Closed 2026-08-18, and it was never a defect — the spec was reading the wrong element.** The task recorded that "a notice the user needs is overwritten by the next solve diagnostic", written up from `dev/browser-pass/specs/files.js` failing on "and says why". It is not overwritten: `setNotice()` writes `#lpn_map_notice`, the transient box over the map, and the solve diagnostic writes `#lpn_status` and never touched it. The spec read `status()`. That IS the remedy the task itself prescribed — "shown somewhere that is not the same one line" — already in place. The spec now reads `notice()` and passes, with the confusion named at the call site as well as in the helper.
- 0|422| The units strip became TWO GROUPS, 2026-08-18 (Tom: "Splitting the inputs is much more satisfactory"). Inputs decide what the document's numbers MEAN and ask before changing — a warning naming the fields that unit serves, with Reinterpret (the standing rule, and the default) and Convert (which rewrites Base AND every scenario override). Results are pure display and change with no fanfare. Flow, elevation/head and pressure serve both sides and are duplicated: `lpn_u_r_flow`, `lpn_u_r_elevhead`, `lpn_u_r_pressure`. Older files name no result key, so a missing one falls back to its input twin at BOTH moments — reading a file, and reading a page whose selects are absent. Harness `dev/lpn-spike/unit-split-harness.js` (7 mutants) and browser spec `dev/browser-pass/specs/units.js`.
- 0|423| Patterns reach the solver, 2026-08-18. `js/lpn-patterns.js` now loads BEFORE `js/lpn-inp.js` in `Looped-Network.php`; `docFromInp()` carries `patterns`, `defaultPattern`, `times` and `controls` onto the document (and `serializeProject()`/`applySaved()` keep them); `assembleModel()` multiplies each junction's demand by its resolved multiplier at t=0. The resolution is `node.demandPattern || doc.defaultPattern` and the default is deliberately NOT written onto the junction — the file never stated it there. Measured against EPA's own published Net3 report at 0:00: worst head error went from 41.04 ft to **0.005 ft** across 97 nodes. `lpn_inp_drop_demand_pattern` replaces the old sentence, which claimed the demand was the file's number; `head-pattern` keeps the old key because a reservoir head pattern still is not applied. Harness `dev/lpn-spike/pattern-demand-harness.js`.
- 0|424| The units strip wraps each selector onto two lines, 2026-08-18 — the name above, the control below (Tom, twice). Side by side, eleven name+select pairs are as wide as their two halves added together and were the widest thing in the new Settings box; stacked, a pair is as wide as its wider half. `.lpn-units-item` in `Looped-Network.php` + `css/engcalcs.css`; measured in `dev/browser-pass/specs/units.js` as name-above-control plus "the box does not scroll sideways".
- 0|425| Shipped 2026-08-24. The input-unit question is now Tom's own 2026-08-18 wording and has the CANCEL the first cut lacked: *This unit decides what your inputs mean*, then *<unit> is the unit of what you enter for:* with the field names ONE PER LINE, then *Options for units change:* and the two sentences, then **Non-destructive / Destructive / Cancel**. Non-destructive is first and is the suite's standing rule; Destructive is the opt-in and says out loud that it loses the original inputs and that Undo puts them back. **An EMPTY project is asked nothing** -- the test is `countUnitValues(name) > 0`, the exact count of what a conversion would rewrite, so a project with elements but no number in that quantity is silent too. **The open backup question is answered NO SECOND COPY:** a Destructive change is a user-initiated edit and gets what every edit gets, one snapshot on the one 20-deep stack (Task 184: one document, one undo stack); a separate backup would be a second document with no home and a second question inside a dialog whose job is to ask one. What was actually missing was HALF AN UNDO -- the snapshot carried the numbers and not the UNITS, so Ctrl+Z returned the original values under the NEW unit and silently landed the user in the Non-destructive outcome they had just declined; `saveUndoSnapshot()` now records `readUnitSelections()` and `undo()` restores it. The one limit, stated in the code: switching projects clears the stack, after which the originals live only in the file on disk. The lead is a NEW key (`lpn_units_warn_lead`) rather than an edit of `lpn_units_warn_body`, because the field names left the sentence and 26 translations would have kept a `{list}` the page no longer fills -- `lang_tag_parity_check.php` is blocking on exactly that. Guard: `dev/lpn-spike/unit-change-harness.js`, mutation-tested against five mutations (Cancel applying the unit, the snapshot dropping the units, always asking, a comma list, Non-destructive converting). The `$ec_lang_syn` proposal the block carried is still Tom's to approve and is now Task 503.
- 0|426| Shipped 2026-08-23. The shared "Set units:" row now carries one tip on the whole label (`calc_set_units_tip`), so every calculator including `lpn_` gets it from one edit. It borrows Task 425's word **Non-destructive** verbatim so the row and the planned lpn units dialog agree, then glosses it in Simple English — "A 6 stays a 6, but it now means 6 inches instead of 6 millimetres" — because a translator cannot recover the term of art from the words. Tip on the label, not on each button: two tips would put two `?` glyphs in one short row. A proposed `$ec_lang_syn` entry is recorded in Task 425 and awaits Tom's written OK. This makes the existing non-destructive behaviour LEGIBLE; the destructive/non-destructive CHOICE is still Task 425.
- 0|427| Already done — the map does say which field it is coloured by. `renderColorLegend()` in `js/looped-network.js` sets the legend heading to `colorFieldLabel(group, field)` plus the unit in parentheses, so the legend names both. Closed 2026-08-22 on Tom's reading: "I think this is done or obsolete. We have a legend."
- 0|428| THEMATIC MODE NO LONGER HIDES TEXT, 2026-08-21. The conflation was not in `applyLabelVisibility()` but in a SECOND path: `applyThematicMode()` put `.lpn-thematic` on the `<svg>` and `css/engcalcs.css` hid `.lpn-thematic .lpn-lbl` — and `.lpn-lbl` is worn by generated labels AND by the user's Text, so a selector could not tell authored content from annotation. `applyThematicMode()` is deleted; `applyLabelVisibility()` is now the single seam (`dataLabelsHidden = georefActive() || settings.colorThematic`) and hides `.lpn-annotation`, whose membership `annotationEl()` declares at the build site. A "Temporarily hide all" checkbox was added here and REMOVED on 2026-08-22 — Tom found it duplicated the Thematic map's own suppression and could not find it by its name; a bug fix is not the place to introduce a new user-facing control. `lpn_settings_color_thematic_tip` said "Hides every label", which was the same falsehood in words, and was corrected. Guard: `dev/lpn-spike/label-visibility-harness.js` section 4 READS `css/engcalcs.css` and asks whether any hiding rule would fire on the element given the classes the JS applied — asserting a class name would have been the stub-removes-the-coupling trap, since the defect lived in the join between the two files. Verified by re-introducing the defect shape: exit 1, then 0 restored.
- 0|429| Deleted at Tom's word 2026-08-22 ("I think we are good. Delete this task."), by ruling and not by shipping: the Ranges picker keeps its four break boxes plus Equal intervals / Equal counts / Automatic. The one fact worth keeping is that the other half DID ship — the ramp picker and its catalogue of 41 ramps in Brewer's three families in `js/lpn-ramps.js`, each published at 3-7 classes, rainbow kept last and never the default.
- 0|430| "the inputs have been converted" on an `.inp` round trip — **and they had not been.** Measured: no unit converted, no value changed, 1,225 tokens with the same value. The 34 differences were 32 tokens' TEXT (`9.00`→`9`, `.1`→`0.1`) plus 2 renamed pump curves, because every gallery example JSON predated Task 390 step 3 and carried ZERO `tok` bags. Fixed by backfilling them (`dev/scripts/backfill_example_tokens.js`, re-runnable, 74 bags) and by keeping a pump's curve NAME through the importer and `docFromInp()`. Now 1,229 of 1,229 byte-identical on both legs, guarded by `dev/lpn-spike/inp-roundtrip-net3-harness.js`. Diagnosis: `dev/inp-export-conversion-bug.md`.
- 0|431| The examples gallery is dismissed PER PROJECT, 2026-08-18. One page-level `galleryDismissed` could not answer a per-tab question — opening an example cleared it for everybody, so the next empty tab focused got the shop window again, which was Tom's "sometimes". Now keyed by project id, in memory beside `tabViews` (a fact about the session, not the document), and **seeing content IS the dismissal**: a project that has ever had a network never shows the wall again, emptied or not. Deleting your network is not a request for the catalogue.
- 0|432| no window scrollbar on the lpn page; the form's margin-bottom was collapsing into the document's scroll height. The tab strip's own phantom scrollbar was a separate overflow-y default, fixed 2026-08-21.
- 0|433| Path chooser shipped 2026-08-24 as the Google Directions gesture: click the start node, hover to see the candidate leg drawn dashed on the map, click to add a stop, double-click to finish, Escape to abandon. A leg is the shortest path by total link length through the ACTIVE graph, using the same length the solver is given; an unreachable stop is REFUSED and both node ids are named rather than committed-and-broken. Recovery is structural, not a stash -- the gesture writes only into `draw.stops` and never touches `from`/`to`/`waypoints` until it commits, so dropping the state IS the restore. Hover-driven, so there is no touch equivalent and none was invented: touch keeps the From/To pull-downs. Guarded by `dev/lpn-spike/profile-chooser-harness.js` (44 checks, six mutations, every one caught).
- 0|434| both panes shipped: bottom #lpn_pane (Profile, Junctions) in normal flow, right #lpn_rpane as an overlay, kept empty after Task 441 took its contents. No left pane, ever. Closed at Tom's word 2026-08-21.
- 0|435| Closed 2026-08-24 on Tom's own pass (*"Done. Close."*). `columnHeadings()` had centred all four Labels-panel headings, but the Before/After columns hold left-aligned text boxes, so a centred heading read as sitting right of its column; affix headings are now `start` (not `left` -- RTL) and numeric headings stay `center`. Column geometry is proved by `dev/lpn-spike/label-columns-harness.js`, which builds the real Node and Link lists and agrees to the pixel.
- 0|437| Folded into Task 477 on 2026-08-22, which is the startup wizard that gives the geocoder its home. Tom: "There's not a lot of reason for search once you are at your model. Projects aren't that big. If somebody wants it, we can add it later." So a standalone search interface is ruled out for now; `js/lpn-search.js` keeps its one entry point in the placement tool, and the wizard enables a place-name search box when lat/lon is chosen.
- 0|438| Sprint 438 ran 2026-08-19: 26 agents, one per language, 20 keys each, all Sonnet. Closed with `detect_english_drift.php --baseline-new` plus a per-key re-baseline of the 17 CHANGED keys it covered; the 6 still flagged are post-sprint English and are Task 459. It found three TOOLING defects, none in the translations: the identical-to-english gate fired on a title that is a proper noun plus a mode name and pushed three agents into distorting a correct value (hr invented a phrase, bg swapped a comma for an em dash, de renamed a correct 'XY'), all reverted and both keys exempted; `glossary.json` claimed lat/lon and XY are carried unchanged into every language while 10 of 26 files already translate `lpn_geomap`, and agents quoted the false rule back all sprint; and the real rule -- one name per language across the dozen strings naming a mode -- is now `dev/scripts/mode_name_check.php`, deriving its key list from the English. Translator findings: five languages independently separated Run from Play where the glossary's root would have collided them.
- 0|440| The link half of the label pass is three passes -- write every label, measure every label, then iterate the shed CASCADE a rung at a time across all of them (`shedToSegmentBatch`). Close of the 256-junction grid went 18,106 -> 8,473 ms with the two builds run ALTERNATING, because this number swings by a factor of two with what else the machine is doing. The proof that it is the same drawing is a COUNT, not a clock: `dev/lpn-spike/label-batch-harness.js` re-runs each cascade the old way and requires an identical decision, and forced layouts fell 1,033 -> 10 on a 112-pipe grid. What is left is no longer measurement -- extracted as Task 472.
- 0|442| Closed 2026-08-24, WORKED AROUND rather than built. Tom: *"I think we have worked around this one. I have been using the phone in tall mode, which I think is best, and for that, putting the menu on the side isn't great."* So the toolbar stays where it is, on every screen, and the wider-than-tall responsive rule is not wanted. What the ruling produced instead is a wording change: the public blurb now says the page works on a phone **in tall mode**, which is narrower and more honest than the claim it replaced.
- 0|443| Three English defects sprint 438's translators found, fixed and resynced into all 26 languages 2026-08-19. `lpn_field_text_match_pipe` (a verb with no object — six languages stopped), and `lpn_time_running`/`lpn_time_no_engine`, which said "EPANET engine" and so reintroduced the pump-motor collision sprint 252 was run to repair. The fourth, `lpn_time_format_tip`, was the real find: it taught `30 minutes` as typable, but `EC.lpnParseTime` accepts only the literal English tokens, so 18 of 26 languages shipped an instruction their own parser rejects. Fixed as a DESIGN fault — the tip now teaches only locale-neutral forms (`2:30`, a bare number meaning hours) and never mentions the word form, because a keyword inside a translatable sentence guarantees the mistake recurs. One finding is still referred to Tom in `dev/english-friction/438-wave0.json`: "8 is eight hours" is ambiguous where the clock counts from dawn (am, sw).
- 0|444| `EC.lpnParseTime` read `2,5` as 2 hours — half the value, no error, in the field that sizes the whole run. Fixed 2026-08-19 with an `opts.typed` flag: a comma decimal is accepted from TYPED input only, as `<digits>,<digits>`, and every other comma shape returns null so the field snaps back. A `.inp` file's separator is always a dot, so a comma in a file is not a decimal and guessing at one would rewrite the user's own number. Six assertions in `dev/lpn-spike/pattern-clock-harness.js`.
- 0|445| deprecated 2026-08-21. Inverting label priority to "drop first" was not worth a stored-document migration that would silently reverse the meaning of every saved number.
- 0|446| Zoom no longer repaints colour at all — `refreshSymbolSizes()` (the zoom path) ended in `refreshValueColors()`, and colour follows the document, never the scale. The three paths that install a different document call it themselves instead. Breaks are now an argument to `paintNodeColor`/`paintLinkColor` rather than recomputed per element. Measured on a 220-link grid: `breaksFor()` per repaint 343 → 4 (O(1): also 4 at 40 links), per wheel notch 686 → 0. `dev/lpn-spike/color-perf-harness.js` counts the calls and asserts all 341 painted colours are byte-identical; mutation-tested against HEAD.
- 0|447| "Convert to lat/lon" replaced by opening. Opening always makes a NEW TAB, which dissolves merge-versus-replace and the disabled-row question. File rows: Open… (the project file's own `project.coords` decides), Import EPANET file… (`[BACKDROP] UNITS` decides -- DEGREES opens lat/lon, everything else XY), and `Import xy to lat/lon…` LAST because it is the fallback. **No prompt anywhere**: Tom proposed asking when UNITS says None, and EPA's own Net1/2/3 all say None, so that would have been a modal on every EPANET import. `js/lpn-inp.js` had been discarding `[BACKDROP] UNITS` entirely; it now exposes `mapUnits` plus `mapUnitsRaw` so absent and None stay distinguishable. The fallback row takes BOTH file kinds, routed on content not extension, and serves two different operations: coordinates readable as degrees are REINTERPRETED where they stand (the wizard arms at step 2, nothing is written through arithmetic, and pressing Keep untouched returns them byte-identical), while out-of-range coordinates are PLACED at the world view as before. One new key, `lpn_georef_asdegrees` -- the two paths state different facts and one sentence true of both would say neither.
- 0|448| Colour band limits FREEZE for the project (Tom, 2026-08-19: watching an animation, "the colors are temporarily stable in meaning", and recomputing per time step gives a wrong impression of the system). Built once with a separate snapshot field to honour the unit paradigm, and Tom rejected that the same day: "I see one set of breaks, and it's manual. But you can push a button to fill them using a method... The project doesn't care where it came from other than 'I found it in the input box; I don't know how it got there.'" FINAL: one field, `settings.colorBreaks`, holding the numbers whoever put them there; choosing a method or pressing Automatic FILLS them; typing any limit stamps mode `manual`, displayable and stored but offered by no dropdown. Criterion methods are never stored (SI thresholds converted per display unit) and nothing is written before two distinct values exist. Projects saved under the rejected design migrate on open. A button the user pressed is not the silent rewrite the unit paradigm forbids; that rule is unchanged. 155 harness checks, six mutants caught.
- 0|449| `lpn_time_format_tip` reworded to Tom's own 2026-08-19 wording, which covers the clock-start field and the six durations alike: "Enter times and durations as decimal hours (17.5 or 72.5) or in hours:minutes notation (17:30 or 72:30)." No second key was needed. Queued into sprint 438 for the 26 renderings.
- 0|450| The Run button now shows a box: a real progress bar, a completion report, and EPANET's own run report. Progress is `t / duration` straight off `runH()`'s returned simulated clock -- monotonic by construction, and the harness asserts it is that ratio so a later "improvement" to a frame-counter proxy fails. The obstacle was not the number but that the loop was SYNCHRONOUS, so nothing could paint; `js/lpn-epanet.js` now runs it in 100 ms slices with a `setTimeout(0)` between, which also makes a run non-blocking. **Slicing changes no number and it is asserted, not claimed:** `sliceMs:1` against `sliceMs:1e9` gives bit-identical heads, worst difference exactly 0. The EPANET report is reachable after all -- `setStatusReport(1)` then `readFile('eps.rpt')` after `close()` -- 4.7 KB for Net3, shipped verbatim, nothing composed by us. Three new keys. A box opens only for a run the USER asked for, and a superseded run takes its box with it rather than freezing a part-filled bar.
- 0|451| The page now says which EPANET it runs: OWA-EPANET 2.3.5 of 2025-02-20, printed as `2.3.05` because the engine writes a two-digit patch, reached through epanet-js 0.9.0 (MIT, Luke Butler). The user-facing home is a Notes entry (Help > Notes on this page) rather than the run report, whose UI lives in `js/lpn-time.js`; `js/vendor/README.md` carries the same lineage for a reader of the source. It reads like a fabrication beside EPA's own 2.2.0 download and is not one -- development moved to Open Water Analytics after EPA's 2.2.0 of Dec 2019.
- 0|452| Never blocked, and had not been for some time — the title was simply stale. `lib/config.inc.php` defines `EC_MAPBOX_TOKEN` with Tom's live public `pk.` token, and the second tile source, its View row, its attribution (Mapbox and Maxar as well as OpenStreetMap) and the no-token fallback to the street map all shipped. `privacy.php` names both tile hosts. Closed 2026-08-22.
- 0|453| Arriving at a project cost 30-50x more if you came from a lat/lon one — Tom reported it twice and the first answer ("it is Net3's own open") was wrong. Measured 2026-08-19: reaching Net3 from a blank XY project 405/568 ms, from Net3-World 14,616/22,398 ms, and a CPU profile put 97% in `span()`/GC/`addSegment` in `js/lpn-collide.js`. Cause: the label reach comes from label sizes (`textSize / state.s`) and the obstacles are the drawing, with nothing tying them together, so a carried-over lat/lon scale made labels microscopic while pipes stayed thousands of units long and one pipe was indexed into (L/cell)^2 grid cells. `grid()` now raises its own cell — the safe direction, since a bigger cell only offers the narrow phase more candidates to reject. 16,446 → 583 ms. Guarded by `dev/lpn-spike/collide-harness.js` (bounded AND still agrees with the scan) and `dev/browser-pass/specs/crossproject.js`.
- 0|454| Net3-World published as the gallery's first lat/lon example 2026-08-19 (Tom built it): `examples.json` entry `net3_world`, keys `lpn_ex_net3_world_title`/`_desc`, in sprint 438.
- 0|455| The bottom pane has seven tabs: Profile, then one table per asset kind (junction, reservoir, tank, pipe, pump, valve) in the toolbar's own Add order. Text gets none — nothing about it solves. Tom chose seven-and-let-them-wrap over a type selector. One generic `renderPaneTable(spec)` replaced the junction-specific renderer; a type declares itself as a row of `buildPaneTables()` and the tab strip is generated from that list, so tabs and tables cannot drift. Per-type sort state; results columns read-only and right-aligned; every write ends in `completeEdit()`, the popup's own seam. 8 new keys and 3 reworded, not the 15 a naive build needed — the six singular type names already existed, and one generic empty-message and one generic tab tip serve all six. Wrapping turned up two real layout bugs, both measured at 520x900: the pane's height clamp was WRITTEN BACK to `paneState.h`, so narrowing and re-widening permanently shrank the pane, and `applyPaneLayout()` clamped against the layout `applyMapHeight()` had just replaced, running the page 60 px past the window. 46 new browser checks; pane harness 137.
- 0|456| Net3-World carries a time model again: Tom re-saved it from Net3 via Convert to lat/lon and it now holds a 24-hour duration, 5 patterns and 6 controls, so Run, Play and the step selector have somewhere to go. The product half shipped 2026-08-19 (a project with no duration disables the transport controls and says why, via `lpn_time_no_period`); this closes the half that was always Tom's, and `generate_examples.php` redeployed the served copy and its thumbnail.
- 0|457| "Overline" and "underline" are back on the page, in the high/low mark row's tip, so the Settings box search finds them by the words Tom asked for. Plain English still leads and each term is a parenthetical gloss -- the shape of "Minor (local) loss" -- rather than a return to the pre-Wave-0 wording. `$ec_lang_syn` was the other candidate home and it is invisible to that search, which decided it. `specs/visibility.js` now probes both words, not one stand-in.
- 0|458| Content moved into Task 459 on 2026-08-22, when the three sprint tasks were consolidated into one. 14 mode-name disagreements in ru, sr, tr and zh, named exactly by `dev/scripts/mode_name_check.php`.
- 0|460| Time modelling has an INTERFACE now: a Libraries box, borrowing the Settings box's whole shell so there is one box design on the page. **Patterns** are one field of multipliers plus a live step-line sparkline -- a step line because a multiplier holds for its interval and a smooth curve would draw a demand the solver never applies; space/comma separated so an `.inp` line pastes straight in; renaming cascades to every junction's `demandPattern` and to `doc.defaultPattern`. **Controls** are the sentence itself, because `lpnParseControl` accepts exactly EPANET's four shapes and a structured editor that could not express one would be worse; a live verdict reads Understood / Not understood / names an id this network does not have. **Curves** is a VIEWER: points live on the pump and editing here would be a second writer of one field, so what it adds is every curve side by side. A junction popup gained a Demand pattern selector, without which an authored pattern could only be the project default. 46 harness checks and 32 browser checks; the load-bearing one is that the demand the engine applies moves in the ratio the edited series states -- wanted 3.2000, got 3.2000 -- which fails if a pattern is carried faithfully and then ignored.
- 0|461| deprecated 2026-08-21. The suite-wide element→asset rename was dropped rather than spend a sprint on it; the tabs keep saying PARTS.
- 0|462| The water-network toolbar group shipped 2026-08-20 in Tom's order -- Libraries, Settings, Run, Step back, Play, Step forward, Time, Speed -- with Settings moved out of the right-hand end group. Nothing floats on the map (Task 463). `js/lpn-time.js` was not touched: `lpnTimeMountToolbar` is handed the shared group instead of making its own.
- 0|463| Proposed 2026-08-20 that the gear and the bottom-pane toggle become controls floating on the map rather than toolbar buttons, and REJECTED by Tom the same day: "Toggles on map doesn't sound right to me. What we have now plus the lpn group (Libraries, Settings, Transport, and Time selectors) seems like the right way to go." Settings therefore moves into Task 462's water-network group rather than onto the drawing surface. Recorded so the idea is not re-proposed: the drawing surface stays a drawing surface.
- 0|464| Hide map readouts left the toolbar for the View menu alone, 2026-08-20 (Tom: "Relegate Hide map readouts to the View menu"). The other two halves were already answered: Wave 0's rename made the control name the act, and with an honest name the footer strip it does not hide is a design decision (Task 253) rather than a false claim. `dev/browser-pass/specs/cleanmap.js` now asserts the toolbar does NOT carry it.
- 0|466| A control naming an element that no longer exists is dropped in `EC.lpnTimeModelBlock`, beside the conditionless drop it already made. EPANET rejects the whole input over one dangling `[CONTROLS]` line, so a document saved before the element was deleted would not run at all; the page's own handler then fell back to the no-engine steady answer, which is why it looked like "the run just didn't happen". Both sides are checked -- the controlled LINK and, for a level or pressure condition, the NODE it watches. The filter applies only to a document that DECLARES its topology, because a block built straight from a parsed file (`dev/lpn-spike/net3-model.js`) carries controls and no element arrays. `dev/lpn-spike/control-dangling-harness.js` asserts the drop AND that the same control forced back in really is rejected by EPANET, so the guard cannot become untested ceremony.
- 0|467| Automatic recalculation is a PROJECT SETTING now, not a stopwatch. `settings.autoRun` (default ON, because ON is what the measured heuristic already did for every ordinary network) sits under Settings > Calculation > Hydraulics, and ticking it off puts the Calculate button back on the toolbar. `EC.LPN_TIME_AUTO.budgetMs` no longer vetoes anything: a slow run is advised against instead, over the new `EC.LPN_TIME_SLOW_MS` (1 s, not 400 ms -- 400 is where a page stops feeling live, which is right for a silent decision; interrupting somebody needs the point where the cost is undeniable). **The advice NAMES the switch rather than offering a button, because the status bar is textContent** -- a clickable offer needs status-bar machinery that does not exist. `dev/lpn-spike/time-harness.js` had asserted the old silent veto and now asserts the inversion: a slow network keeps re-running AND says so, and only the user's answer stops it.
- 0|469| SHIPPED 2026-08-24. Node labels now shed properties before one of them is hidden, so `labelSettings.priority.node` is a real drop order in both lists. Placement runs first and the shed is the rung after it: `shedNodeLabelsForCrowding()` takes the labels `placeLabelsFirstFit()` DROPPED, adds the placed labels whose reserved rows block them, drops each one's lowest-ranked property, re-measures and re-places, at most four rungs. **Both sides of the pair shed, not only the loser** — in a first-fit the loser is by construction the lower-ranked one and the ground it needs is held by the WINNER, so shedding the loser alone narrows it into a space that never opens; measured against the loser-only variant on the same views, 41 shedding labels and 96 of 97 drawn against 17 and 93, and on a 480-pipe grid 50 drawn against 39. The ID is never shed and was never in the column; a label sheds down to its ID plus one ranked value and then goes whole, which is the terminal rung `nodeDropKey()` already owned. ANY overlap, not a vertical one — `js/lpn-collide.js` has no notion of an axis. What it buys, in node labels DRAWN on Net3-World: 17→33 at s=5000, 45→73 at s=12000, 81→96 at s=30000, and 20→50 on the grid, with the total number of values on screen unchanged or slightly up (396→402 at s=30000) — the trade buys named nodes for a few numbers. Cost: the content pass runs 1.05–1.7x (Net3 173→183-200 ms at s=30000, 157→265 ms at s=5000; the grid 720→878-963 ms) and a DRAG FRAME is unchanged, because the cascade is gated to the two callers that pass `relayoutLabels(true)` — refreshLabelTextPass() and the debounced scheduleReshed() — which is the same content/position line Task 399 drew for the link half. Blockers are found through `Collide.grid()` rather than a pairwise walk, for the reason yieldStationedLabels() gives. `lpn_labels_priority_node_tip` was rewritten to the agreed wording (English only). Guard: `dev/lpn-spike/node-shed-harness.js`, 31 checks, async against the real EPANET engine (Task 496) — without `await settleEpanet()` the whole fixture labelled an unsolved drawing carrying only `[id,demand,elev]` and two assertions failed, which is how these numbers were nearly re-fitted rather than re-derived. Five mutations caught: loser-only, reversed drop order, sheddable ID, no unshed (the ratchet, visible only on the zoom path), and no cascade at all.
- 0|470| Duplicate, closed 2026-08-23. Search-by-place-name SHIPPED 2026-08-19 under Task 437 (`js/lpn-search.js`, commit `5e0bb176`) — Nominatim on ENTER, one request per second, nothing cached, behind its own `ec_geosearch` consent gate separate from the analytics banner. Task 470 was raised 2026-08-21 from a fresh look at epanet-js and repeated a blocker that was already decided: its line "the OSM tiles are currently the only [third-party request] the suite makes" had been false since the geocoder landed. Its one live remainder — the geocoder as the placement wizard's FRONT DOOR — is Task 477, which is where 437 was folded.
- 0|471| A refusal and an unreachable engine are now different things. `js/lpn-epanet.js` resolves a refusal as `{refused: true}` via `engineRefusal()` and reserves REJECTION for "no engine"; before, both left by the same door and the page's one handler called `noEngine()`, so a network EPANET would not accept produced our own solver's answer with nothing said — and told the user to connect to the internet, which is an hour wasted with the engine sitting right there. Applied to `lpnSolveEpanet` too, which was printing "Did not converge" for a refusal. The user hears three things on the `lpn_unit_unknown` model: EPANET would not accept this network, the engine's own words verbatim, and that the numbers came from the built-in solver. Also closes Task 466's silent-drop gap: `lpnTimeModelBlock` emits `{code, ids}` warnings in the shape `lpnToInp` already used. Six keys; `dev/lpn-spike/engine-refusal-harness.js` runs the real engine so the refusal assertion cannot go stale.
- 0|472| Closed 2026-08-23, the last of the quadratics. `alignedSideFor()` walked every link to decide which side of its own pipe ONE label hangs on; the drawing now goes into a uniform segment grid once per layout pass (`Geom.buildSegmentIndex`, held the way `mapBox()` holds the canvas box) and each query walks outward from its own cell until the ring is further off than the best distance already found. Measured as a COUNT, not a clock, on the 480-pipe grid `dev/browser-pass/specs/perf.js` uses: **`linkPointList()` calls in one `relayoutLabels()` pass fell 682,052 -> 6,184**, and 38,414 -> 1,452 on the 112-pipe one, with every one of the 480 labels landing in the same place to the character. The index is EXACT, not an approximation, which is what makes it an optimisation rather than a change to the drawing — a label on the other side of its pipe would be a defect, not a saving. The stopping rule is `(r - 1) * cell`, not `r * cell`: the query point sits somewhere inside its own cell rather than at its centre, and off by one there misses the nearest pipe silently, on one label. A segment spanning many cells goes in a list that is always checked instead of being filed into every cell it crosses. New guard: `dev/lpn-spike/aligned-side-index-harness.js`, which requires the index to agree with `pointToPolylineDistance()` over every link and counts the calls at two network sizes. Next in the profile is `readMapBox()` at 16% and the collision geometry at about 13%, neither of them quadratic.
- 0|473| Canal Seepage's two currency inputs are prices PER a unit, so they convert by the RECIPROCAL of the unit factor; both were read with `readFormInput()`, which divides, making every money answer wrong by the factor SQUARED and wrong only under the US preset (SI factors are all 1). Fixed by a new additive `EngCalcs.readFormInputPerUnit()` beside `readFormInput()`, which is unchanged — the direction for a rate is a suite-level fact, not a canal-seepage one. Annual value lost went $50,608.53 -> $63,115,200. `'separator' => '/'` is the structural marker of such a field and greps to these two lines alone, so no other calculator was exposed. `cs-harness.js` asserts it, including US/SI invariance at 1e-6; reverting one call fails 4 checks.
- 0|474| Manning Irregular's region Froude number mixed a region area with the LAST SEGMENT's top width, because `closeRegion()` swapped in `pwc` and `ac` while nothing ever accumulated a top width. Added `tc` beside them: a region's top width is the sum of its wet segments'. The shipped sample section went 0.28 -> 0.554; single-segment regions are unchanged, which is the proof it was not fixed by breaking the working case. `fr618` is corrected by the same line. `mi-harness.js` asserts both shapes; dropping the fix fails exactly 2 of 66 checks.
- 0|475| Closed 2026-08-23, and the reported NaN was the small half. **A zero-length segment is a VERTICAL WALL** — zero area, zero top width, and a wetted perimeter equal to its submerged height `|d0 - d1|`, correct whether both ends are drowned, one is, or neither. The duplicated-station NaN is just the degenerate wall of zero height, which now contributes nothing instead of poisoning its region. The real find: the old `pw = (a == 0) ? 0 : ...` test could not tell a wall from a DRY bed, so both walls of a rectangular channel contributed no perimeter — a 10x5 ft box reported P = 10 ft, R = 5 ft instead of P = 20 ft, R = 2.5 ft, overstating Q by 2^(2/3) = 1.587x on the commonest section there is. A wall's roughness now also enters the Horton-Einstein composite. `js/manning-irregular.js` only; `js/Manning.lib.js` untouched, since nothing else shares this geometry. `dev/calc-spike/mi-harness.js` gained a rectangular channel asserted end to end and a subdivision-invariance check — a duplicated station must leave the shipped sample numerically UNCHANGED, which is strictly stronger than "no cell says NaN". Its own reference `segPerimeter()` was wrong for a wall too. Mutation-tested: the old code fails 10 new checks, `Q got 362.07, want 228.09`. The disclosure question is Task 490.
- 0|476| Done 2026-08-22, and the stale TITLE is why nobody noticed: there is no "Convert to lat/lon" any more. The File menu row is `Import XY to lat/lon…` (`pickGeoFile` in `js/looped-network.js`), which takes a FILE and lands it in a new project, so nothing mutates in place and the user's XY file is never one Save away from being overwritten.
- 0|478| Closed 2026-08-24 on Tom's *"Please merge"*, which is also his ruling on the one open question: a `<td>` wraps and two grid columns cannot, so below the fold the label column now absorbs the squeeze instead of the unit select dropping to a second line. `echoInputGrid()` emits the input lines column-major and places each cell with `grid-row`; **x-cross went from 16/12/9/8/8/7/7/7/6/5/5/4/1 to 0 on all 15 grid pages**, and `focus_order_check.php` blocks on any page that renders `.ec-fieldgrid` and is not at zero. Layout is pixel-identical in English at 1400px on all 16 pages and in all five RTL languages (`dev/browser-pass/fieldgrid-layout.js`). Round 2 answered Tom's three follow-ups: the four dynamic tables (mi, wi, bpn, ip) tab by COLUMN and the last top input goes straight into the table's first cell — done as a Tab key handler (`EngCalcs.wireColumnTabOrder`), not a DOM order, because those are real `<table>`s built at runtime by one shared `addCalcRow()`; the Printable titles were already tab stops, with a measured 4-stop detour after them, reported and left. Accepted cost, stated not hidden: WCAG 1.3.2, since visual and DOM order now differ for the top fields. Guarded by `dev/browser-pass/specs/tabcolumn.js`, 36 checks; removing the wiring fails 20, disabling Shift+Tab alone fails 12. Tom's own browser pass is Task 501.
- 0|480| PRIVACY DISCLOSURE CORRECTED 2026-08-21. `privacy.php` said "exactly one exception" and there were three third-party requests: OpenStreetMap tiles, Mapbox satellite tiles, and Nominatim place-name search. The one paragraph became four, with the search given its own — a tile says where you are looking, a search says what you typed, which is why it has its own `ec_geosearch` gate, and why "we send them nothing about you" now sits inside the TILES paragraph only. Four further gaps were found in the same page and were the bigger finding: the `ec_geosearch` cookie and five `lpn_*` browser-storage keys (`lpn_identity`, `lpn_pane`, `lpn_rpane`, `lpn_setbox`, `lpn_show_titles`) were not in the storage table at all; the `share` usage signal was undisclosed; and "the two items that need your permission" was a miscount of three. `dev/cookie-storage-inventory.md` was itself missing `api.mapbox.com`. Nothing was removed or narrowed. Left alone deliberately: `ec_nolog` (still has no UI control), and the "Nobody" sharing paragraph, which is about data we collect and remains true.
- 0|481| Closed 2026-08-23, both halves. The check shipped as advisory `dev/scripts/stale_claim_check.php` — every `Task N` in `CLAUDE.md` and `dev/*.md` cross-referenced against the closed ledger and ranked by whether a negation sits on the same line, which is the shape of the three false "not built yet" claims. Then the first worklist was actually worked: of 14 HIGH findings, 7 were legitimate past-tense records and 5 were false present-tense claims, rewritten to current state — the `.inp` writer's missing sections (three of four now ship), the scope doc calling `lpn_` a PREVIEW page in violation of CLAUDE.md, a Phase 3 that had become `lpn_` itself, and "do not promote Task 146" advice for a task that closed. HIGH 14 → 11, all 11 records. The class of defect is permanent — closing a task deletes its block, so nothing re-scans the prose that quoted it — so the check stays advisory and the worklist gets re-run, not re-invented.
- 0|483| PER-ASSET IMPORT NOTES SHIPPED 2026-08-24. Every `.inp` difference that names an element is now filed on that element as an `importNotes` record and read back in its property popup under "From the EPANET file" — the import report was one dialog, read once, after which the fact that THIS pump lost its curve was gone. Stored as `{code, detail}`, never as a sentence: `inpDropText()` composes the English at display time, so a project imported in one language reads in whatever language its reader is using and the popup and the report cannot drift. Found while building it: **an EPANET id is unique only within its own kind** — Net3 holds a junction 123 and a pipe 123, and 3 of its 4 element-naming differences name an id that exists twice, so a bare-id map filed a junction's demand pattern on a pipe as well; `drop()` now states the kind at the moment the difference is found. Also new: a Text whose `[LABELS]` anchor names a node not in the file was silently forgotten and now keeps that name in a note. The anchor half of Tom's steer was ALREADY BUILT (a Text follows a node, stored as an offset); anchoring to a LINK is Task 502. The Meter is noted, not reproduced, as he asked. Guard: `dev/lpn-spike/import-notes-harness.js`, 364 checks, mutation-tested six ways; `.inp` export is byte-identical with the notes on the document and without them.
- 0|485| Shipped 2026-08-23. **The directory name stays `spock/<YYYY-MM-DD>/`; the archive registers ITSELF.** `archive_logs.php --apply` now seals `.archive-manifest.json` into each archive — schema, ending date, provenance, the PREDECESSOR archive's name, the window and per-log row counts. Rejected: encoding the window in the directory name, the obvious "systematic naming" answer, which orphans every archive already on production, is a claim no code verifies, and duplicates the manifest. A name a human types is a convention; a manifest a script writes and a script reads is a mechanism. `--verify` walks every archive oldest-first and answers the real question — **is this hole a boundary or a loss?** A gap in traffic is a note; only an unrecorded gap in DATA exits 1, so it is cron-safe. `trim_logs.php` now SIGNS its deletions into the manifest, without which a retention trim and silent data loss look identical — and the trim is the part meant to run unattended. `lang-log-stats.sh` grew a PROVENANCE line and accepts a bare `--archive=2026-08-14`. Nothing renamed or moved: a hand-moved archive still reports, is still trimmed, and reads as `derived`. `dev/scripts/log_archive_manifest.inc.php` is the one opinion about the format. A paste-ready crontab is DOCUMENTED and NOT ENABLED in `dev/usage-data-log.md`; `spock/public/.htaccess` is still commented out pending Tom's yes.
- 0|486| Shipped 2026-08-22. One CSS breakpoint at `max-width: 640px` (the width the project tab strip already used, so the page reorganizes once on the way down, not twice), scoped `html:has(#lpn_canvas)`. Hides the three page-title ids, zeroes the navbar padding while keeping the hamburger — the only route to the other 15 calculators AND to the language picker — hides every toolbar group but the three `.lpn-transport-btn` controls and their two selects, and hides the menu word while `aria-label` carries the same existing key at every width. No new lang key, no new file, no `matchMedia`. Edit > Select was ADDED: hiding the Select tool would have orphaned it, since Insert could enter add-junction mode and nothing but Escape left it. Guard: `dev/lpn-spike/small-screen-harness.js` reads `css/engcalcs.css`, builds the real menu bar and toolbar over Net3, and asserts hidden at 360px AND visible at 1200px. Its own trap: Help > "What the toolbar icons mean" is derived from the toolbar and gives every button an inert menu row, so "reachable from a menu" was a tautology until inert rows were excluded. Tom's phone pass came 2026-08-23 — "For today's standards, we are gold" — and the sanctioned public claim is his own: "Try it. We did."
- 0|488| Fixed 2026-08-23, and the cause was structural rather than cosmetic: the Print button was a SIBLING of `#lpn_pane_tabs`, so it held a flex column of its own and the tabs could only wrap inside what was left. A new `#lpn_pane_strip` wraps the tablist and `.lpn-pane-tabs` became `display: contents`, so tabs and button wrap as ONE flow and a second line starts at the pane's left edge at full width. The X stays outside the strip and pinned top-right; the button stays extreme left per Tom's 2026-08-21 ruling. Neither candidate the task proposed was used — icon-only and move-to-overflow both trade away discoverability Tom asked for, and neither fixes the wrap. `pane-harness.js`'s two "tab strip wraps" assertions were retargeted from `.lpn-pane-tabs` to `.lpn-pane-strip`; left alone they would have passed on a strip that cannot wrap at all.
- 0|489| Shipped 2026-08-23. The satellite credit set gained the two things Mapbox requires and we lacked: their WORDMARK, and `Improve this map` -> `apps.mapbox.com/feedback/`. The two links we already had were verified to point where they specify. Scope was the satellite set only; the OpenStreetMap street-map set is untouched. **The logo is Mapbox's own dark-on-light artwork taken verbatim from `mapbox-gl.css` as a `data:` URI** in `.lpn-mapbox-logo`, 65x17 px at their stated width — embedded, never fetched, because a request to Mapbox before the user turns satellite on is exactly what `#lpn_basemap_teaser` was built to avoid. `dev/lpn-spike/basemap-credit-harness.js` asserts the four required parts by exact label and URL, that the CSS rule names no host, that no `src`/`srcset` on the page points at Mapbox, and that no tile host contains "mapbox" after the boot path. The strings are hardcoded English, matching the existing `(c) OpenStreetMap contributors` treatment — Mapbox specifies the attribution text — so no `$ec_lang` key was added.
- 0|490| Closed 2026-08-23 the way Tom asked — *"Add an errata to the Notes to be honest"* — rather than by a notification nobody has a channel for. `mi_notes_3` ships on Manning Irregular carrying the date it was found and fixed, what was wrong, who is affected and what to do. **His question about the shape of the defect was measured, and the assumption in it was wrong in a reassuring direction:** it was a CLIFF at exactly zero horizontal run, not a degradation approaching vertical, and orientation was irrelevant — with `l === 0` the slope is forced to 0 and the area is `d0 * l` = 0 whether the bank rose or fell, high point or low point. Probed numerically: at a horizontal run of 1e-9 ft the old code already returned the exact perimeter, so an "almost vertical" bank was never affected. Submergence is what mattered — the loss is the SUBMERGED height of the wall, so a wall standing above the water was always correctly zero. The errata says all of that in plain words. No `$ec_lang_syn` entry was written; that array stays Tom's.
- 0|491| FIXED 2026-08-23. A Darcy-Weisbach e and a Manning n both printed on the map as "0": `defaultLabelSettings().decimals.link.roughness` shipped as 0, which is right for a Hazen-Williams C and for nothing else the page offers. The places now come from `defaultRoughnessDecimals()` — two significant figures of the method's own typical roughness READ IN THE UNIT NOW DISPLAYED, because e moves three orders between millimetres and feet and one per-method constant would be wrong at one end. `syncRoughnessLabelDecimals()` follows that default until the user types on the Labels spinner and then stops following, remembering its own last value in `roughnessDecimalsAuto`; since that starts at the shipped 0, a project saved carrying the bug is corrected on open while a chosen count is kept. A simpler rule was tried and is wrong: "raise it when the typical roughness rounds to zero" leaves 1.5 mm at 0 places, because 1.5 rounds to 2. Guard: `dev/lpn-spike/friction-method-harness.js` §9, asserting the PRINTED STRING in four method/unit combinations. The [H] browser question about input width left as Task 495.
- 0|493| Closed 2026-08-23, and most of it was already done. **The centring shipped earlier the same day** (`046c9216`): `.lpn-pane-table th, td` centre and `.lpn-pane-first` does not, with the input at `text-align: inherit` so the box follows its cell. Of the 13 widths Tom listed, **11 already matched** — strong confirmation that his figures are multiples of the 7em every box used to be. Two had drifted and were corrected: Diameter 2.8em → 2.1em (0.3), and Minor loss 2.1em → 1.4em (0.2). **The Minor loss change REVERSES his own earlier ruling that day** — 2.1 existed to pass his acceptance test ("'2.5' needs to work okay": 1.4em shows one of three characters) and he reversed it on the ground that *"inputs are flexible. You can enter more than their width."* **That is untested in a browser and he asked to be reminded** (Task 491). `text-align: left` on the first column became `start`, so RTL leads on the right. New guard: `dev/lpn-spike/pane-col-width-harness.js` holds the declared list as the AUTHORITY and asserts both the JS `em:` values and the stylesheet note against it — the drift existed precisely because one intent lived in two files, and a deliberate override and a missed column looked identical from outside. Its brace-balancing body reader is deliberate: a character-capped regex stopped at an inline `unit: function () {}` and reported three columns as having no width at all.
- 0|494| Closed 2026-08-23 on Tom's *"spock: Make it reachable"*. The grant in `spock/public/.htaccess` is on; only the aggregate is served and the raw per-event logs stay denied by the parent `.htaccess`, because publishing rows would contradict `privacy.php` and cost a `consent_body` rewrite, 26 retranslations and an `EC_CONSENT_VERSION` bump. **The grant alone publishes nothing, and that is the part to know:** `.gitignore` tracks spock/'s directories and `.htaccess` files and ignores the DATA on purpose, so the report is not carried to production by `git pull` — it must be generated ON production from production's own logs, by hand or from the cron block in `dev/usage-data-log.md`. Until then the URL is a 404 with the door open. Verified locally end to end: the script writes the private pair and the redacted served copy. `publish_usage_report.sh`'s "NOT REACHABLE YET" docblock was substituted, not appended to.
- 0|495| Closed 2026-08-24. Tom: *"Yes. This was never in doubt. Visibility is the only limit."* An input accepts more characters than its width shows, so a pane-table column width is a VISIBILITY limit and never a limit on how many decimals a value can carry. Nothing rode on the answer by the time it came: round 3 of the width review had already widened the last two narrow boxes past their longest values.
- 0|496| CLOSED 2026-08-24. `dev/lpn-spike/lpn-dom-stub.js` now loads the real vendored EPANET engine, so `EngCalcs.lpnSolveEpanet` exists and `runSolve()` routes exactly as a browser does. It genuinely runs headless — Node's dynamic `import()` takes `js/vendor/epanet-js.js` from a `file://` URL, ~16 ms to load, ~35 ms for the first solve — which `validate_epanet.js` and `eps-net3-harness.js` had been doing all along; the only thing Node cannot supply is the browser's site-absolute default url, filled in stub-side. No product file changed. New `dev/lpn-spike/engine-route-harness.js` (24 assertions) proves the setting reaches the engine by PHYSICS rather than by a flag: Chezy-Manning is where the two engines knowingly disagree, so 'epanet' must land at 0.9939–0.9944 of our head loss and a fall-back reads 1.0000 — mutation-tested by returning the native answer under EPANET's flags, which fails it. It also covers both halves of the valve rule: a PRV routes itself to EPANET with `settings.engine` left on 'native' and the status bar naming V1, and with the engine deleted the native solver refuses by name. `stub-fidelity-harness.js` gained a fifth property so the engine cannot silently go missing again. Fallout, which was the point: the shipped gallery example carries `"engine": "epanet"` and had been solved natively in every harness that opens it — `gradient-label`, `label-affix` and `readout-sign` were reading numbers that are now asynchronous and went to 13 failures between them; each is now `async` with an `await settleEpanet()`. No defect in the product code was exposed.
- 0|497| SHIPPED 2026-08-24. A geographic project can read every node's ground elevation from Mapbox Terrain-RGB and TYPE IT IN — `js/lpn-terrain.js`, one View-menu row, hidden without `EC_MAPBOX_TOKEN` exactly as the satellite row is. Height decodes client-side from the pixel (`-10000 + ((R·65536 + G·256 + B)·0.1)` m); one request per TILE, not per node, so forty nodes on a site is one or two; the zoom steps DOWN from 14 to a 24-tile budget rather than clipping the extent, stops at 10 so the stated accuracy stays true, and past 200 tiles refuses in words. **Its own consent gate, `ec_terrain`**, modelled on `ec_geosearch` and separate from it on purpose: a tile says where you are LOOKING, a search says what you TYPED, and a node coordinate says where your NETWORK IS. Only a yes is ever stored; a refusal writes nothing; removed by Settings > Erase everything. **`consent_body` was NOT touched and `EC_CONSENT_VERSION` was NOT bumped** — the banner asks about one analytics digit and says nothing about third-party requests, and `dev/cookie-storage-inventory.md` already recorded the rule that makes this legitimate: *"nothing leaves until you switch that feature on, and each asks separately — so a fourth service is a new paragraph, not a contradiction."* `privacy.php` gained its fourth paragraph and an `ec_terrain` storage row in the same commit, and the count claims in `CLAUDE.md`, `dev/positioning.md` and `dev/cookie-storage-inventory.md` went from three to four with it — a stale count is the class of false claim this repo keeps catching. The judgement call worth knowing: `addNode()` seeds every node with `settings.defaults.nodeElev` = 0, so a freshly drawn network has no BLANK elevations at all, only a number nobody typed — and a seeded 0 is indistinguishable from a typed 0. Those are offered only once nothing is blank, in a confirm that NAMES the number it would replace, rather than being swept in silently or refused outright. Accuracy is stated in the INTERFACE three times on the path a person walks (menu tip, confirm, result notice), written once in `accuracy()`. One undo snapshot per command, so one Ctrl-Z reverts the whole fill. Guard: `dev/lpn-spike/terrain-harness.js`, whose tile stub returns BYTES encoded by Mapbox's own formula from an independent inverse Mercator, so the tile index, the pixel index and the decode all stay under test; nine mutations caught, including a typed elevation overwritten between the request going out and the answer coming back.
- 0|499.01| Closed 2026-08-24. The Scenario readout in the map's status strip now carries an icon: one plan sheet branching into three, `'scenarios'` in `lib/Icons.lib.php`, drawn to Tom's own brief ("three branched children of a main project... squares with an aspect ratio representative of a 2H x 3H plan sheet") -- parent 9x6, each child 6x4, every rectangle 3:2. The Project icon's detail was dropped, the fallback he offered in the same note: drawn with a title-block band and measured at 17 px with `icon_ascii_preview.php`, the parent's 5-unit interior closed to about one pixel and the sheet read as a solid block. It goes on through `setLabel()` inside `refreshScenarioStatus()`, which rebuilds the readout on every scenario switch, so the old bare `textContent` assignment would have wiped it.
- 0|499.02| Closed 2026-08-24. `lpn_menu_project_tip`, and `buildMenuBar()` learned to wire a tip at all -- title plus `.ec-help` plus `initTips(bar)`, the same three things a menu ROW gets in `openMenu()`. Project is the only menu-bar item with one, because the other five are the words every application uses. Tom's brief was *"Everything unique about this application is here in one place except the animation play controls"*; the shipped value names what is in there and sends the reader under the map for Play. **His "would be good to have this reviewed by Mary before or after deployment, either way is fine" is NOT done** -- it is a wording review of one English string, not a blocker, and it is recorded here because a closed block is never re-scanned.
- 0|500| CLOSED 2026-08-24. Every reference-table URL now lives once, in `lib/References.lib.php`, reached through `ecRefUrl('name')` and `ecRefRoughnessUrls()`. Accessor FUNCTIONS rather than a bootstrap global because Manning-Irregular, Branched-Network and Irrigation-Pressure build their row tables inside a function, where a global is not in scope and the symptom is a silent `href=""`. The block named six pages; the true set is FOURTEEN, and Micro-Hydro-Power carried the 490-character `nepis.epa.gov` string the block said was in five places. One real divergence was found and reconciled: Manning's n was written `http://` by Manning-Pipe-Flow, Manning-Pipe-Head-Loss, Manning-Trap and Manning-Irregular and `https://` by Branched-Network and Looped-Network; https was kept, since every other engineeringtoolbox link in the suite was already https. All five `nepis` copies were byte-identical. Rendered HTML of all 14 pages diffed against HEAD: 10 byte-identical, 4 differing only by that one scheme. Still outside this file and wanting their own task: the orifice C_d URL embedded inside `or_notes_3_def` in all 27 lang files. `dev/lpn-spike/popup-tips-harness.js` had to be repointed -- it scraped the URLs out of `Looped-Network.php`'s literal source, which is exactly the copy this task removed.
- 0|501| Closed 2026-08-24. Tom's browser pass on the Task 478 field grid: *"Done. Close."* The four pictures no harness could answer -- the X button hiding all four cells of a line, the conditional lines on Orifice, Orifice-Drain-Time and Branched-Network appearing and disappearing whole, "Printable version" then toggling the orifice shape keeping the X's gone, and the collapse animation still animating -- all pass in a real browser.
- 0|506| Shipped 2026-08-24. The profile has NO side interface: the From/To pull-downs, the "Choose the path on the map" button, the waypoint chips and Remove-all are gone, and **the Profile button itself is the way in** -- Tom: *"Our button to create a new path can be the Profile button. That removes all our left-side controls."* One press shows the profile, a second (while it is showing) arms the chooser, a third cancels; Escape still cancels for a pointer. What is left in the panel is ONE line of running commentary, which is now the only thing that knows the gesture exists, so it is never empty -- idle it says how to start, half-drawn it says what the next press does, refused it names the two nodes. The control column became a header ROW in CSS alone (the markup is untouched), so the chart takes the pane's full width. **TOUCH IS TOM'S OWN GESTURE** (*"short tap for tentative (hover), long tap for 'Add waypoint', and Double tap for end"*): short tap = the candidate, PROFILE_HOLD_MS = 450 ms = commit, double tap inside PROFILE_DBLTAP_MS = 400 ms = finish. The one place the two agree rather than mirror is the FIRST stop, which commits on a plain tap because there is nothing to preview before there is a stop to route from. The browser's own dblclick arrives after a touch double tap and is SWALLOWED, or it bends the pipe under the finger. **The Task 417 long-press collision does not arise**: the chooser's long press is reachable only while the chooser runs, and while it runs the map starts no element drag at all (panning still works, and is how an off-screen node is reached now the pull-downs are gone). **Deliberate losses, for Tom to rule on:** changing ONE end of an existing path, and removing ONE waypoint -- both are now "draw it again". Eight lang keys are left rendered by nothing (`lpn_profile_from/to/pick/through/draw/draw_stop/clear/choose`) rather than deleted, because deleting one retires 27 strings. While the chooser runs the map also opens NO property popup on a miss (a press on a pipe mid-gesture was opening one over the drawing) and starts no element drag. Guard: `dev/lpn-spike/profile-chooser-harness.js`, 76 checks driven through the real listeners on a fake clock so both thresholds are testable from both sides, eleven mutations, every one caught.
- 0|507| Closed 2026-08-24, the same day it was written. **49 keys**: every visitor-facing string in `js/lpn-terrain.js` and `js/lpn-search.js`, consent dialogs included, is now in `lib/lang.ec.en.php` and in `Looped-Network.php`'s pageConfig. No JS logic changed — both files already read `t('key', 'English literal')`, so the literals stayed put as the fallback and the keys simply lit up. **The one design decision: a consent dialog is ONE KEY PER PARAGRAPH, joined with `\n\n` in JS.** No `$ec_lang` value in this project has ever held a newline, so the alternative was inventing a line-break placeholder inside one wall of text; four short keys read better to a translator and keep the paragraph ORDER ours. `lpn_terrain_consent` became `_consent_1..4`, `lpn_search_consent` likewise, and `lpn_terrain_confirm_default` split in two. `{n}` is substituted across the whole joined consent text, so a language may say how many nodes are about to be sent even though the English says it in none. `pageconfig_check.php` cannot see any of these (both files use a bracket lookup, not `EngCalcs.pageConfig.<key>`), which is exactly why every site keeps its English literal. Tom's [H] question — whether to hide the terrain row until it was keyed — lapsed unanswered because the fix landed first. Now sprint material: Task 459.
- 0|459| CLOSED 2026-08-24 by running the sprint. **26 languages, 152 keys each, 3,962 in all**, Sonnet throughout, 20 agents at once and 6 as slots freed. Wave 0 first: 219 keys reviewed, 12 findings, **nine English rewrites** — three of them in the two permission dialogs Task 507 had just keyed, where "May we use it?" had no antecedent standing alone and now names what is sent and who receives it. Four more findings came from translators, two of which were real English defects nobody had seen: `lpn_terrain_requests` was a CONCATENATED FRAGMENT (Turkish and Bengali filed it independently, two unrelated grammars, and the tell was that its own siblings carried a `{n}` and it did not), and `lpn_georef_asdeg_btn` said `lon/lat` against two neighbours saying the other order — Tom ruled **lat/lon everywhere**, which also turned the status readout and the property popup round. Mechanics cost more than translation did and are recorded in `dev/translation-process.md`: agents share one scratchpad path and one clobbered another's script mid-run; two keys already present in every target file were listed in every payload and cost ten-plus agents a wasted cycle; and editing the English mid-sprint left nine later agents reading a payload that disagreed with the source. Three cognates exempted by language rather than globally. Closed with `detect_english_drift --baseline-new` (147 keys). Unfinished parts extracted to Task 513.
- 0|526| Fixed 2026-08-25. `js/lpn-epanet.js` split lpnToInp's report in two: `signatureWarnings`, which cannot change while `signatureOf(model)` holds, and the full `warnings`, which is those plus `valueWarnings(model)`. Only the first is cached on the open session; the second is recomputed at every solve, so `minor-loss-gravity-differs` — raised from `lpnLinkK(link) > 0`, a value the signature cannot see — now follows the current `k`. Took the roadmap's second option: computing per solve rather than putting "any k > 0" in the signature, because it is a fact about current values and belongs where current values are read, and the alternative rebuilds the whole session whenever a minor loss crosses zero. **The costly direction was the silent one** — adding a minor loss to a network that had none announced nothing at all. `dev/lpn-spike/engine-note-once-harness.js` section 4 inverted from pinning the defect to asserting the fix, both directions, and counts `lpnToInp` calls to prove the warm session was reused across them (0 rebuilds) — without that the test would pass for the uninteresting reason. The stale comment that claimed the invariant is replaced, not appended to.
- 0|439| Built 2026-08-25, Option B as Tom ruled it. A geographic document is now rebased onto an origin DERIVED at load and never stored: `chooseGeoOrigin()` floors the model's extent onto a **1/128-degree power-of-two grid**, `rebaseGeoDocument()` shifts every stored point in the drawing frame between the projection and the flip, and `rebaseLiveGeoDoc()` does the same the moment `georefFinish()` makes a document geographic — compensating `state.tx/ty` and the remembered tab view itself, so a frame change cannot move anybody's map. **AND THE FILE FORMAT DID NOT MOVE, which is better than the (a)/(b) the task framed.** A geographic file already stored absolute lon/lat with `origin` {0,0}; writing {0,0} back is byte-identical to v10, so there is no v11, no migration, and a file saved now still opens in a page that predates this. **The exactness argument in the task was INCOMPLETE and the mercator harness caught it:** Sterbenz makes `x - ox` exact only while x is NEAR ox, and a document may span the globe — a node at latitude 1e-7 came back as 1.0000000116860974e-7 and one at -0.1 as -0.10000000000000707. Fixed by carrying the file's own value on BOTH axes (`_xsrc` beside the existing `_ysrc`) and testing it against the SHIFTED number inside `unprojectStoredGeo(o, org)`, which is now the one place the origin comes back off a coordinate; `view`/`backdrop` get the plain add-back because they are ours, and it cannot accumulate. Two guards did real work rather than being updated for form: `local-origin-harness.js` refused a first draft that called `cartesianY()` directly (a fifth frame boundary — routed through `outwardX`/`outwardY` instead), and `mercator-harness.js` found the Sterbenz hole. New guard `dev/lpn-spike/geo-origin-harness.js`, 27 checks, including bit-identical open-and-save and a stable second round trip. Browser pass 961/961.
- 0|522| Built 2026-08-25, reversing Task 422 on Tom's ruling: *"we shouldn't have separate input and output units."* `LPN_UNIT_SELECTS` is now one list of eight in strip order; `LPN_INPUT_SELECTS`, `LPN_RESULT_TWIN` and `fillResultUnitDefaults()` are gone, `LPN_RESULT_UNIT`/`resultUnit()` are KEPT as the greppable seam every solved number is read through. Not a straight merge — velocity and gradient were always results-only, so "one set" is the input set plus the two that never had a twin. **The reconciliation rule for an old file carrying both maps: THE INPUT UNIT WINS**, stated once in `reconcileLegacyUnits()`, because the input unit says what the numbers stored in the file MEAN while the result unit only said how answers were printed — taking the result unit would silently reinterpret every elevation, demand and valve setting, which is the failure CLAUDE.md marks absolute. A wrong reading is visible; a reinterpreted model is not. One selector now decides both, so `lpn_u_roughness` is finally stored in a project. `dev/lpn-spike/unit-set-harness.js` (46 assertions, mutation-checked: reversing the priority produces 4 failures) and a rewritten `specs/units.js`. Two orphaned keys, `lpn_units_group_inputs` and `lpn_units_group_results`, ruled LOST by Tom and deleted from all 27 files.
- 0|504| Built 2026-08-25. `dev/scripts/generate_features.php` merges `dev/features-source.md` with the closed ledger into `dev/features.md`, wired into `check_all.sh` as a freshness check like the roadmap index. **A closed entry does not declare itself a feature; a hand-written companion file does** — the rejected alternatives are in the generator's docblock, and the decisive one is that 135 of 448 ledger entries have no text at all to derive from, and among the rest `SHIPPED` opens both features and internal plumbing. 53 features citing 87 closed IDs; the generator fails if a cited ID is not closed or is claimed twice. Prose is a first pass awaiting Tom's edit. Placement ruled by Tom: the LibreWaterNet landing page, not Help.
- 0|520| Fixed 2026-08-25. `georefGoTo()` read metres-per-degree at the DESTINATION latitude on both sides of one division, so the latitude cancelled and the model silently changed size by `mpd.lon(hanging)/mpd.lon(destination)` — the reason nothing had caught it, and the reason changing only one of the two would have broken working code. It now takes the first at the transform's own origin latitude and the second at the destination, both on `mpd.lon`, which is Task 517's seam. **The spec came first and was watched FAILING before the fix**: `dev/browser-pass/specs/goto.js` measures the model's screen width across a Go-to — 334 → 398 px at 38 N, and 626 → 314 px going from 60 N to the equator, the predicted factor of two. All three read 334 px now. The complementary check (screen width is worth the ground width typed) passed before and after, which is exactly why this hid: the DOCUMENT was always written at the right ground size and only the view scale was wrong. Found by `/code-review` during Task 517, not by a harness.
- 0|527| Closed 2026-08-25, every bullet answered. From Tom's first real phone session, screenshots 0034-0042. Two items he ruled NOT defects (the map showing nothing while the bottom pane is open — *"I don't think it's realistic to expect to see more than one thing at a time"*; and the legends, which Task 529 answered with an Off placement). Three fixed in CSS at the existing `max-width: 640px` breakpoint: the Settings index became a horizontally-scrolling strip of whole names above the content (8 of 14 names had been splitting a word; content pane 238 → 320 px), the label-symbology headings got `overflow-wrap: anywhere` (the boxes already fitted — "Before" simply painted out of its 33 px box into "After"), and the roughness column went to `break-word`, costing 28 px because "Roughness," measures 94.4 px whole. **The profile axes turned out NOT to be a label-count problem**: `profileLayout()` floored the viewBox at 240x180 before writing it, so `preserveAspectRatio` fitted a 344x180 drawing into 115 px and drew the whole chart, text included, at **0.64** — every 10 px label came out at 6.4 px, invisible to the stylesheet because the font-size was right and the transform was not. The viewBox is now the host at every size; `fitTicks()`/`labelStride()` in `js/lpn-profile.js` thin gridlines and station names to fit; and the summary line is written BEFORE the chart is measured, which stops a feedback loop that had the phone chart permanently laid out for a panel it was never shown in. At 360 px: scale 1.000, y labels 7-at-5.7px-apart → 3-at-29px, plot frame 172x34 → 271x58. Costs on a phone: four y gridline numbers, one node name, and the station-axis title (the length unit still appears on the summary line). Guards: `dev/lpn-spike/small-screen-harness.js`, `dev/browser-pass/specs/smallscreen.js` (measuring where the ink lands in a real 360 px Chromium, mutation-checked), `dev/lpn-spike/profile-harness.js`.
- 0|294| Closed 2026-08-25 by Tom's ruling: **delete.** Of the seven keys this task held for his decision, two had already resolved themselves — `mi_d50in` and `contact_title` were gone from `lang.ec.en.php` and survive only as names inside dev scripts, and `mpf_spreadheet_notice` had been renamed to `mpf_spreadsheet_notice` under Task 322. The remaining five — `menu_main_list`, `menu_main_language`, `mpf_spreadsheet_notice`, `wi_save_and_calculate`, `or_shape` — were verified dead first (`or_shape` looked live in `Orifice.php` and was a PREFIX COLLISION with `or_shape_circular`/`_rectangular`; no exact-key use exists anywhere) and then deleted, 140 lines across all 27 files including the `$ec_lang_syn` entries, payloads regenerated. Key hygiene's dead list fell 21 → 16.
- 0|523| Shipped 2026-08-24, confirmed 2026-08-25. `lpn_menu_project` now reads **Water** — the KEY deliberately keeps its old name, because renaming it across 27 files buys no user-visible gain and `lpn_menu_project` is still what the menu bar's element is called. Tom's own reasoning, arriving at it in three moves and staying: *"'Water' is just better all around. Fun to use, fun to teach, fun to own, fun to share. It's why we chose our careers. Mni wiconi. Water is life."* The icon went with it — a drop first, then a peaked-roof water tower after he struck the drop for reading as a tear beside the View menu's eye.
- 0|248| Closed 2026-08-25. Extended-period simulation SHIPPED 2026-08-18 through the EPANET engine (`js/lpn-time.js`, `EngCalcs.lpnEpanetRun`), checked against all 25 steps of EPA's own `Net3.rpt` to 0.005 ft over 2,425 head comparisons. Patterns on a reservoir head and on a pump speed followed 2026-08-24 (248.02). The built-in solver has no time dimension and is not getting one; with EPANET unreachable the page solves one instant and says so. What remains of the original scope is `[RULES]` alone, which is Task 248.03 and now carries its own priority — so this parent has nothing left to hold.
- 0|306| Closed 2026-08-25 as OBSOLETE, not built. Tom ruled 2026-08-24: *"Keep both, but EPANET is silent."* LibreWaterNet.org is the front door and LibreEPANET.org gets no page at all, so the rebranded site variant this task specified has no reason to exist. Verified live the same day: `libreepanet.org` 302-redirects to `librewaternet.org`, which is the ruling working. The domain stays bought.
- 0|307| Closed 2026-08-25 as OBSOLETE, with 306 and for the same ruling: LibreEPANET.org gets no front door, so there is no front-door copy to approve. The shape Tom recorded for it — an invitation to build, not a pitch to switch, and no comparison to anyone — survives in `dev/positioning.md`, which is where a future page of any name would read it.
- 0|355| Closed 2026-08-25 as OBSOLETE on Tom's own reading, 2026-08-15: *"I think we are good, to tell the truth. Nothing to do, I think."* Nothing was ever scheduled. `linkLabelTooShort()` still hides a short pipe's label all-or-nothing, and if that ever reads wrong in practice **Task 399 already covers it** — the label sheds values instead of vanishing whole, which makes `linkLabelTooShort()` the last rung rather than the only one. A WAIT-AND-TEST task with a successor is a task with nothing of its own left.
- 0|347| Closed 2026-08-25, DECLINED rather than built, on the argument the block already carried. `init()` guarantees an invariant in as many words — *"the library always has exactly one open project, so there is never a state where drawing has nowhere to be saved"* — and a tabless boot breaks it everywhere at once: autosave, the dirty asterisk, tab restore. Tom's strongest form of the examples gallery (*"It's not a map until the first project is started or opened?"*) is answered instead by Task 314's gallery, which shipped. Kept out of the ledger as a decline so it is not re-proposed from scratch.
- 0|194| Closed 2026-08-25 as OBSOLETE on Tom's ruling: *"Scroll is obsolete. Task is obsolete."* The idea was one finger scrolling the page and two panning the map, raised 2026-07-31 after the canvas-fills-the-phone lock-up. It never became a defect: `applyMapHeight()`'s height cap already prevents the trap, so this was an improvement rather than a fix, and the page the finger would have scrolled is not the shape it was in 2026-07. `#lpn_canvas` keeps `touch-action: none` and the app owns every gesture on the drawing, which is what the map editor needs.
- 0|248.03| Rule-based controls, the LANGUAGE and its editor, 2026-09-05. `js/lpn-rules.js` is EPANET's own [RULES] grammar, pure and DOM-free: split, parse, convert, write. It exists because a rule's numbers are in the units of the file the user opened and `js/lpn-epanet.js` writes LPS and metres always, so `IF TANK T1 LEVEL ABOVE 20` (twenty FEET in a GPM file) arrived beside a tank stated as 4.572 and could never fire -- and you cannot scale a number without knowing, clause by clause, whether it is a level, a pressure, a flow, a link setting, a roughness or a bare count of hours. `modelRules()` parses, filters to the rules naming live elements, converts to SI and reports both kinds of drop by name; the bridge writes `[RULES]` after `[CONTROLS]`. **EPANET checks its rule base BETWEEN time steps, so a rule changes nothing on a single-instant solve** -- the engine's behaviour, copied rather than simulated. The editor is a Rules section in the Libraries box, one textarea per rule, an unreadable one kept and marked; `lpnRuleSplit` is lossless so editing one rule leaves every other line byte-identical. The document's own text still round-trips character for character. Harness `dev/lpn-spike/rules-language-harness.js`, anchored on a GPM/feet file so a missing conversion fails loudly.
- 0|248.04| Closed 2026-08-25 on Tom's ruling, and it closes as ALREADY TRUE rather than as declined. His own 2026-08-17 position was that curves never need a separate interface — *"We may be able to avoid curves as a separate interface indefinitely by reporting them and referring to them by the name of their owner node"* — and that is what the page does: a pump curve is edited on the pump (`curvePoints`), `curveRef` names another pump whose points this one copies, and a tank volume curve belongs to its tank. The task described a REPORT and a NAME, and both exist. The one slice that would be new — a live-linked curve table shared across pumps — is `utility-planning-engineer`'s single non-low-ranked slice of Task 465, and belongs there rather than here.
- 0|516| Closed 2026-08-25 on Tom's ruling: *"Why not close or delete? Other tasks have it covered."* The defect was two colour keys stacking over the label legend when nodes AND links are both coloured (screenshot 0030). Task 529 gave both legends an **Off** placement — his own better fix (*"With all the control we have given the user, the legend is of less value now"*) — and Task 527's phone pass then gave them separate default corners, colour key upper right and label legend upper left. Between an Off switch, independent placements and a user who can move either, nothing is left that this task owns.
- 0|515| Closed 2026-08-25 on Tom's ruling: *"It's a long word, and it's equally long in many languages (as opposed to 'water'). I'm not really that embarrassed by the breaking. I say we drop the task. Accept as is."* Two of fourteen English index labels break mid-word. **Hyphenation was investigated first and measured, not argued** (`dev/hyphenation-finding.md`): `hyphens: auto` changes nothing here because `overflow-wrap: anywhere` already takes the break and the dictionary is never consulted; removing `overflow-wrap` so hyphenation can work makes "Visualization" overflow the pane instead; **Chromium does not hyphenate a capitalised English word at all**; 13 of our 27 languages have no patterns, `tr` among them and it is an anchor; and a suite-wide `hyphens: auto` would print wrong hyphens on every partially-translated page, because an absent key falls back to English under the wrong `lang` and nothing marks which strings fell back. The width arithmetic that made it a choice rather than a bug: the index is 6.6rem leaving 90.6px of text and "Visualization" measures 104.2px, so ending the break needs ~7.45rem — the width he had deliberately narrowed twice.
- 0|284| Closed 2026-08-25 on Tom's ruling, and he reversed himself the same day to get there: he first said *"You are right. Promote to 100"*, then *"We can delete it in the name of clutter reduction. If it's not needed, let's eliminate it."* It held two Settings-box follow-ups after the two-pane box shipped under Task 441 — a second sticky level so the current SUB-heading sticks as well as the section (`.lpn-set-sub` has no `position: sticky`, verified), and a narrow-screen collapse. The second was overtaken by Task 527, which made the index a horizontally-scrolling strip of whole names at phone width. The first was never a defect anybody reported; it was a refinement recorded from one conversation, and a refinement nobody has missed in a month is clutter by his own test.
- 0|534| Shipped 2026-08-25. `echoHTMLHead()` emits Open Graph on all 25 rendering pages — `og:type`, `og:site_name`, `og:title`, `og:url`, `og:description`, `og:image` with its type/width/height/alt — plus `twitter:card` as the ONLY twitter tag, because X falls back to the OG title/description/image and the card TYPE is the one thing OG cannot express; duplicating the other three would be three strings to keep in step with no behaviour to show. **No new language keys**: the title is the one the function just built, the description is `$html_desc` (the `<prefix>_main_desc` CLAUDE.md already refuses to duplicate at 520 strings), the URL is `$ec_canonical` so it inherits the host→origin whitelist, and the site name is the translated `menu_brand`. `og:locale` deliberately absent — it wants `language_TERRITORY` and we carry a bare code. **`og:image` carries NO `?v=filemtime`**, unlike every other asset there: networks key their cache on the URL and `git pull` does not preserve mtimes, so a busted URL would change every deploy and orphan every scraped card. Image is one suite-wide `icons/social-card.png` (1200×576, from the landing page's own hero, `INDEX.md`-publishable); per-page cards are worth more but **every publishable frame in the drop is an lpn frame**, so per-page today would mean inventing images or shipping one picture under twenty names — the plumbing is one variable from allowing it. A centre-crop to exactly 1200×630 was tried and rejected on looking at it: it clipped the navbar brand and the pressure legend. Six pages set no description and emit no `og:description` rather than a placeholder. Guard: `dev/scripts/social_card_check.php`, blocking — every page declares a card, the URL is absolute on a whitelisted origin, the file exists and is under 5 MB, and the REAL PNG dimensions match the declared ones; proved by three deliberate breaks (relative URL, moved file, wrong height).
- 0|535| Fixed 2026-08-25, same day Tom reported it as urgent: *"Manning is broken related to the n buttons"*, then precisely, *"It eats the first digit typed when the buttons are on, using the first digit typed to blank the button."* **An EVENT-ORDER defect, not an arithmetic one.** `n_in` carries `oninput="EngCalcs.submitForm();"` as an inline attribute, registered when the element is parsed; `js/manning-trap.js` registered its un-check-the-radio listener at DOMContentLoaded, LATER. Both fire on `input` in registration order, so the calculator ran while the radio was still checked, saw `n_radio !== ''`, and overwrote the box with the computed n — the digit was gone before the radio ever un-checked. Fixed by un-checking on **`beforeinput`**, which fires before the value changes and therefore before either `input` handler, whatever order they were registered in; the `input` listener is KEPT as well, because `beforeinput` is not dispatched for every programmatic or assistive edit and un-checking twice is harmless. **NO HEADLESS HARNESS COULD HAVE SEEN THIS** — `dev/calc-spike/` calls `pageCalculator(objForm)` directly, so there is no typing, no `input` event and no ordering to get wrong; all 74 mtc checks passed throughout. Guard is therefore a browser spec, `dev/browser-pass/specs/mtcbuttons.js` (15 checks), mutation-proved: removing the `beforeinput` line fails 3 of them and prints the symptom itself — `"0.0291030"`, the computed 0.0291 with the typed 030 appended behind it. **Tom's other report — that clicking the rock-size and roughness buttons together blanks both — could NOT be reproduced by clicking alone** in any order or combination, headless or in Chromium; the strong hypothesis is that it was this same defect met while typing, and the spec now covers both buttons on together.
- 0|513| Closed 2026-08-25, all three jobs done and both blocked strings resolved. **The glossary write-back was done from the SHIPPED STRINGS rather than from the lost sprint transcript** — `dev/scripts/glossary_rendering_census.php` counts which rendering each language really uses and how often (2,626 term x language cells: 1,127 clean, 365 split, 376 where the glossary's own rendering never appears), and its findings went into the `translation_notes` of ten terms. It also reproduced the Pashto agent's own method independently. **Mode names went from 21 disagreements to 0** across 26 languages: bg/bn/cs by reverting `lpn_geomap`; tr by fixing the CHECK, not the Turkish (a dative suffix and sentence capitalisation, now compared case-insensitively); ru by rephrasing three declined forms to carry the nominative pair; sr by standardising a three-way split on the anchor. Thirteen files rendered `lpn_georef_asdeg_btn` longitude-first and twelve were reordered. **`ar` was the hard one and Tom pushed back on leaving it** — *"I would push back on ar since we know that latlon, lonlat, lat, and lon exist"* — and he was right: Arabic has both terms and our own file already shipped them; what it lacks is an ABBREVIATION, since Google Maps Arabic, Google Earth and Arabic Wikipedia all write both out. It became `خط العرض وخط الطول`, built from the file's own two incumbent field labels, with **"and" rather than a slash on his instinct** (*"Could a literal 'and' be better than a slash? Let the translator do what's idiomatic"*) — and that form is verbatim the title of Google Maps' own Arabic help page. **`zh` stands as an exception on his ruling**: `经纬度` is longitude-first and is the lexicalized Chinese compound, `纬经度` is not Chinese, and *"we must go with the local custom on zh."* The duplicate `ponding` glossary entries merged with his acceptance; the structural cause — `termIndexByName()` keys by term NAME, so the later entry won every payload and the earlier was delivered to nobody — is now blocked by a duplicate-name guard in `gloss_ref_check.php`.
- 0|538| Fixed 2026-08-25, found by Tom in a screenshot of the SHIPPED DEFAULTS while re-shooting a share card: *"Can you see the bug that is revealed? The tailwater elevation is not plotted on the sketch."* `orDrawSketch()` in `js/orifice.js` guarded the TWE line AND its label with `v.submerged`, so a free outfall drew no downstream water at all — the default page has HWE 105, invert 101, TWE 100, centroid 101.75, so `submerged` is 0 and the tailwater vanished even though it sat at y=162 against a ground line at y=170 and had room to draw. **The guard is now `tweY < gndY` alone.** Three reasons the old one was wrong, the third being the point: the user TYPED that elevation and a sketch that omits its own input hides the user's number; it drew a dry channel downstream, which is a different physical picture from the one the numbers describe; and it **hid the reason for the verdict** — "free outfall" is true BECAUSE the tailwater is below the opening, and drawing both lets the reader see why, which is the whole job of a definition sketch. Guarded in `dev/calc-spike/or-harness.js` on the sketch's own markup rather than on pixels (nothing rasterises headlessly): both regimes emit two water surfaces and a TWE label, and a tailwater below the ground line still correctly draws none. Mutation-proved — restoring the `v.submerged` guard fails "free outfall: two water surfaces are drawn, not one" with `got 1, want 2`.
- 0|503| Closed 2026-08-26. Tom supplied the wording himself, which is the approval the `$ec_lang_syn` rule requires: *"'Destruction' is not a term of art. The synonym could be as simple as 'Does not change the input data.'"* Written as `$ec_lang_syn['calc_set_units_tip']='Changes the unit shown on every field at once (switches the whole page to that unit system). Does not change the input data.'` — his sentence replacing the AI's six-clause proposal, which had piled up *safe, harmless, does not overwrite anything, leaves your entries untouched, keeps the numbers you typed* in an attempt to reach the same idea. **The lesson is the length**: a synonym entry is instructions to a translator, and one plain sentence that passes the substitution test beats five hedges at it. The three orphaned non-English keys the block also mentioned (`lpn_units_warn_body`, `lpn_units_reinterpret`, `lpn_units_convert`) are unreachable and remain for a sprint's 27-file pass.
- 0|536| Closed 2026-08-26. It existed to ask Tom one question — whether the second symptom he reported on Manning Trap (clicking the rock-size and roughness buttons together blanking both) survived the event-order fix in Task 535. It did not: *"536: Yes. Everything is good now."* The hypothesis recorded when the task was written turned out right — it was the eaten-digit defect met while typing, since that bug both un-checked the button and destroyed the number. `dev/browser-pass/specs/mtcbuttons.js` continues to assert both buttons on together, so if it ever returns the pass says so.
- 0|565| Closed 2026-09-01. `js/lpn-epanet.js` returned `converged: true` HARDCODED at both success sites and threw `runH()`'s return away; `js/lpn-time.js` did the same for every frame, which is the path the scrubber goes through — three assertions nobody had checked. EPANET reports an unbalanced system as a WARNING, so the run completes and hands back the last iterate, and the vendored wrapper's `_checkError()` prints any code under 100 to `console.warn` and returns. `convergenceOf()` now asks the engine through `EN_getstatistic`/`EN_getoption` — **EPANET's own three-part test**, relative error against Accuracy plus the head-error and flow-change limits where set — and `iterations` stopped being `null`. **Two things the measurement found that prose had wrong.** (1) EPANET **clamps Accuracy to [1e-5, 1e-1]** as it reads the file, so the `1e-8` this bridge wrote and the comment boasting it was "far tighter than EPANET's own 0.001/40" were both fiction: every run it ever made solved at 1e-5. The default is `1e-5` now and `convergenceOf()` reads the accuracy back OUT of the engine rather than off `hyd`, or a project asking 1e-8 would be called unconverged forever. (2) `If unbalanced: Stop` does **not** make the toolkit refuse — measured, it hands back the last iterate exactly as Continue does, so a Settings row led straight into the defect. `applySolveResult()` no longer shares one branch between a refusal and a non-convergence: an `ok` result is DRAWN, kept in `lastSolveResult`, and the status bar LEADS with `lpn_diag_not_converged_drawn` plus the trials, relative error and accuracy — CLAUDE.md's own reasoning that refusing to report gives the page nothing to draw stands, and now the page says what it is drawing. `converged: null` (an engine too old to be asked) raises nothing: unknown is not a no. `issues: []` was audited rather than left hardcoded — lpnDiagnose() returns early when it has any, and the two statistics that would belong there exist only under pressure-driven analysis, which this bridge never requests. Guarded by `dev/lpn-spike/convergence-honesty-harness.js` (30 assertions, all driving the vendored engine against EPA's Net3): with `Trials 1`, node 10 answers **47.05 m against the converged 50.35 m** — a 3.3 m error the page used to swear by. New English keys `lpn_diag_not_converged_drawn`, `_trials`, `_error`, awaiting Tom's ruling.
- 0|562| Closed 2026-09-01 on Tom's ruling of the same day (*"Match nodes so that we have fewer numbers... nodes need to get precedence always"*). **ONE KNOB: `TOUCH_REACH_PX = 24`**, with `POINTER_REACH_PX = 14` beside it and `reachPx(e)` the only accessor — it replaced `NODE_SNAP_PX`, `NODE_SNAP_TOUCH_PX`, `NODE_GRAB_TOUCH_PX` and the path handle's `tapSlopPx()`, so the flat contradiction (a handle grabbed at 24 against the same pan rival that forced node grabs to 14) is gone by construction. Every object and every question — grab, tap, snap-on-create, Text anchor — reads that one accessor. **It collapsed UP to 24 rather than down**: Tom has reported *too hard to drag* and *too hard to get nodes* and has never reported *too hard to pan*, so up moves nothing in the direction he has complained about; the declaration names the line to move when he does. `HIT_SLOP_PX` (2) is untouched and its declaration now says in bold it is not one of these. **Node precedence OVERRULES a link hit** rather than filling in bare map, which reverses the constraint the Task 417 touch fallback shipped under (*"it can add a hit, never overrule one"*) — both call sites REWRITE that comment rather than appending. **Two departures worth knowing, each argued at `touchNodeOver()`.** (1) TOUCH ONLY: the geometry is the same for a mouse but the hand is not — a pointer has a cursor, a hover readout and sub-pixel aim, and applying this to it would make every pipe within 14 px of a junction unclickable on the desktop for a defect nobody has reported there. The knob stays one knob; the PRECEDENCE is what a finger gets. (2) NOT over everything: a node outranks a link, a link label and bare map, and yields to a vertex handle and to ANY label including its own data label — that label is drawn a few px from its node, always inside 24, so blanket precedence would have made node labels undraggable by finger, a new defect traded for the fixed one. Stated cost: a pipe drawn shorter on screen than twice the reach sits entirely inside its end nodes' reach and cannot be tapped until you zoom in. Also landed: the ghost-click shield moved INSIDE `openPopupAt()` and sizes itself to what is left of the 350 ms window, closing all three open doors with no flag for a fourth to forget; one `hidePanel()` sweeps tips and hides, and all twelve closers route through it (six never swept); `touch-action: none` now comes from `makePanelDraggable()` via `.lpn-dragpanel` rather than a three-id list, so Find and both fire-flow boxes stop being mouse-draggable only, and the CSS comment that named *Find* while its selector named *Library* is rewritten. Guarded by `dev/lpn-spike/touch-radius-harness.js` (§0 reads the SOURCE back: exactly two reach constants file-wide; §4b drives the real handlers with a CONFIRMED LINK HIT under the point rather than bare map, which is the stub-fidelity trap — a harness always handing bare map cannot tell the old code from the new) and a new `dev/lpn-spike/panel-touch-harness.js`, which RUNS `hidePanel()` against a live tooltip because the DOM stub's `querySelectorAll` returns `[]` and a shape-only check would pass over a sweep that had quietly stopped. Deferred on purpose and still named in `dev/phone-interaction-model.md`: merging the SVG's two `pointerdown` and two `pointerup` listeners.
- 0|571| Closed 2026-09-02. **The movable boxes got a stacking model, which they had never had.** Tom reported three things and they were one missing mechanism: a box dragged to the top slid UNDER the menu bar and tab strip; with Find open, Fire flow could not be dragged and a newly opened Settings appeared behind it and was *"lost"*; and nothing brought the box you were touching to the front. Every panel sat at one fixed z-index with the chrome above them all. **This REVERSES Tom's own ruling of 2026-08-24** (*"The menus and toolbars need a higher z-index than the boxes"*, which answered a box dragged up the window swallowing the strip that opened it): his 2026-09-02 ruling is *"They need to cover everything... these boxes must be sky high, far above everything else, like zindex 1000."* What changed in between is that a box no longer ARRIVES over the chrome by accident — it is dragged there, and it comes forward when touched. Do not restore the old order. `raisePanel()` raises on open (through `placePanelForScreen()`, the one seam every standing box is placed through) and on any press (registered in the CAPTURE phase, because the drag handler returns early unless the press is on the panel's own chrome, and typing in Find must raise Find). **THE FIRST ATTEMPT WAS A NO-OP AND LOOKED CORRECT:** every panel carried an INLINE z-index in `Looped-Network.php` (20/22/23), which beats any stylesheet, so `.lpn-dragpanel { z-index: 1200 }` did nothing and only `raisePanel()`'s own inline write ever moved a box. The fire-flow run box exposed it — it does not open through `placePanelForScreen()`, so nothing raised it and it opened at 23, under the chrome, invisible; and a second attempt raised it in `wireFireFlowBox()`, which runs once at page load, so the box the user opens to press Run was raised on top of it afterwards. It raises beside its own `display` now. The inline values are stripped and a check forbids their return. **The band is BOUNDED at 1200–1799 and renormalises**, because a counter that only rises leaves nothing that can be reliably placed above the boxes — which is exactly what buried the tooltips. Full ladder in `css/engcalcs.css`: boxes 1200–1799, menus 1850/1851, modal 1860/1870, tooltips 1900 (above everything summoned, which is Bootstrap's own order), registration bar 3000. Also fixed here: **orphaned tooltips**, a defect older than the report — Bootstrap renders a tip into `document.body` and this page rebuilds box contents constantly, so a rebuild destroys the trigger and strands the tip with no listener that could ever close it. `hideTipsIn()` walks root→trigger and could never see them; they were piling up at 1080 UNDER the boxes, and raising tips to 1900 is what made a pile visible at once. `sweepOrphanTips()` walks the other way and runs at `initTipsIn()`, which all 19 re-init sites go through. Guarded by `dev/lpn-spike/panel-fill-harness.js` (stacking, the ladder, the no-inline-z-index rule, and a 700-raise renormalisation test that proves the order survives) and `tip-behaviour-harness.js` (an orphan created and swept). Both mutation-tested.
- 0|573| Closed 2026-09-02. The `lpn_` resync sprint: 26 languages, 223 missing keys and 97 stale ones each, ~8,300 strings. Gated on a Wave 0 friction pass over 314 keys that filed **136 findings** (30 high) and produced 111 English rewrites before an agent was paid; six proposals were REVERTED because the harnesses turned out to be the record of Tom's own rulings of 2026-09-01/02. The translators' suggestion box then filed **40 more**, and three were English defects no reviewer had caught: `lpn_run_menu_tip` told the user to press a Run button renamed Calculate in the same sprint, `lpn_field_demand_pattern_tip` named the default-pattern control by a label it does not have, and Swahili found `lpn_mode_add_*` translations that had silently DROPPED a whole sentence. Two more were defects in our own guidance: the glossary's `emitter` renderings were the irrigation dripper (refused independently by ru, bn and bg, each of which kept its shipped incumbent), and the three fire-flow terms shipped with empty `translations` maps so every language coined its own. **The session limit killed six agents mid-run and verification beat the reports in both directions:** `ur` was reported FAILED and was complete, while four reported-complete legs had their 97 stale keys merely PRESENT, still holding the old translation, which a key-existence count would have shipped as fresh. `lpn_settings_elev_source_dem` ('Mapbox DEM') is now exempt after five agents invented word orders to dodge the checker and bg refused to and filed the gap instead. `mode_name_check.php` took 28 findings to zero: six languages had translated the `lat/lon` mode name in the anchor while their own sentences kept the Latin abbreviation. Glossary write-back done (anchor renderings for the three fire-flow terms, a new `tag (EPANET)` term the fr/es/hr/bg agents each asked for); drift baseline moved per key with verification, not by --force.
- 0|572| Closed 2026-09-04 on Tom's word. He read the whole list and found nothing left for him: *"I don't see anything that remains for me."* The confirmations it carried are spent.
- 0|567| Closed 2026-09-04 on Tom's word. Vertices mode, looked at and passed: *"I love it. Done."*
- 0|530| Closed 2026-09-04. Fire flow analysis SHIPPED: the whole-system sweep (`js/lpn-fireflow.js`, N solves not N-squared), the report in Tom's own column order with `Failure modes` naming both modes at once, drawdowns measured even where a junction fails at rest, the per-junction required flow through `setProp()`, and the ISO credit reason carried in the interface. Anchored on AWWA M31 and NFPA 291 for the static-pressure definition, which Tom asked be disclosed and now is, on the column heading. The two phases never built -- picking an EPS frame, and the Run concept -- are extracted to Task 578.
- 0|566| Closed 2026-09-04. The EPANET gap closed as far as it goes: water age and source trace (2026-09-01, anchored on EPA's own Net3.rpt, 2,425 comparisons, worst 0.105%), the chemical/reaction mode (2026-09-03, analytic anchor `C = C0 exp(Kb V / Q)` at 0.0007%, ask-or-disclose coefficients, no invented default), and pump energy (2026-09-04, anchored on EPA's own Net3.rpt Energy Usage table to its own two decimals). `[QUALITY]`, `[REACTIONS]` and `[ENERGY]` are interpreted rather than merely carried, and `signatureOf()` was taught to notice a quality change after it silently reused a warm session. The four sections still carried are extracted to Task 579.
- 0|566.01| Closed 2026-09-04, the pump-energy phase of Task 566, given its own id because it is a feature a reader looks for by name rather than a detail of water quality. `[ENERGY]` interpreted: global efficiency, price, demand charge and a price schedule, per-pump price and pattern through `setProp()`, and a report of running time, average and peak kW, kWh and cost. Anchored on EPA's own Net3.rpt Energy Usage table to the report's own two decimals on both pumps, which also pinned two definitions: `average` is over the time the pump RAN, and the integration is per hydraulic step. No default price, and the box says why.
- 0|580| Closed 2026-09-04. Find is a VIEW of the project now, like every other box on this page: `refreshFindForm()` rides `rebuildSettingsBox()`, so a project arriving, a water-quality mode or a friction method repaints the open panel rather than leaving it offering properties that are not there. findNormalize() runs first so a selection that stopped existing falls back instead of rendering blank, and a typed query the controls cannot express is carried across character for character. Six assertions in `dev/lpn-spike/find-harness.js`, driven through `rebuildSettingsBox()` so the WIRING is what is tested.
- 0|576| Closed 2026-09-04. The Settings panes have a draggable divider: a 6px grab strip with a col-resize cursor, `role="separator"` and the arrows and Home for a keyboard, clamped to 48px and to 60% of the panes row, and reversed in the five RTL languages because the index sits on the right there. The spacing is unchanged -- a third flex child earns a second `gap`, and `margin: 0 -8px` pays it back, which the small-screen harness measures. **NOT REMEMBERED**: a stored pane width is a per-visitor preference and Tom's to rule, so the drag ships and the memory waits one field away in `lpn_setbox`. `dev/lpn-spike/setbox-divider-harness.js`.
- 0|577| Closed 2026-09-04. Pump energy is NAMED a report and stays where it is: `lpn_energy_menu` and `lpn_energy_title` become `Pump energy report`, and the ellipsis goes because nothing opens asking for input. The planning-engineer seat answered from the work -- a pump-energy figure is a deliverable for somebody else (a preliminary engineering report's operations section, a life-cycle cost) rather than a column watched while drawing, which is what the Tables pane is for; EPANET's own Report > Energy is standalone for the same reason. Journal entry 2026-09-04 in `dev/agents/utility-planning-engineer/`.
- 0|584| Closed 2026-09-04 on Tom's word (*"I agree. Close it."*), his own position adopted verbatim: **a new project gets the hard-coded defaults, always; if you want otherwise, save a template or copy a project; window furniture is not project data and follows the browser.** A documentation task, because the split was already there and already right -- modelling data rides in `serializeProject()`, furniture is a `localStorage` sibling key (`lpn_pane`, `lpn_rpane`, `lpn_setbox`, `lpn_findbox`). Written into `CLAUDE.md` and `dev/looped-network-calculator-scope.md`, and said in the interface at the one place it is decided: a tip on the New project chooser's units fieldset (`lpn_new_units_tip`). The gap he named answers itself -- furniture is already where you left it, so a template opens into your own layout without the file carrying one. **One case the code does not meet is recorded and not changed**: `buildNewBoxUnits()` copies the open project's units into the chooser, which is a setting inherited from another project. It is shown before Create is pressed, and flipping it changes the commonest control on the page, so it is Tom's call.
- 0|531| Closed 2026-09-05. Tom's three axes are tags on every example in `examples/manifest.json`: `system` (us/si) and `coords` (xy/geo) DERIVED from the project file itself, `work` (design/maintenance) declared by hand in `exampleWork()` in `dev/scripts/generate_examples.php`, because nothing in a network file says whether it is being designed or being run. `--check` re-derives all three from the files rather than comparing the manifest to itself, so a tag that stops matching its example fails the build, and an example nobody has tagged is reported instead of shipping untagged. **NOTHING IS DISPLAYED and no lang key was added**: a displayed tag is a string in 27 languages, and the gallery has no grouping or filter to display one in until Task 348. The `work` values are AI's reading and Tom has not ruled on them: `design` for the two basics and Elm Street Center, `maintenance` for EPANET's four samples.

- 0|582| Pump efficiency curves are kept, written back and honoured. `PUMP <id> EFFIC <curve>` ran at the GLOBAL efficiency, so kW, kWh and cost were wrong by the ratio of the two; the points now ride on `doc.inpSections.CURVES`, reach the model through `docEnergy()`, and go to the engine as `EF_<pumpid>`. Anchored at a hand-computed 62.5% and on the flow axis in both unit sets (`pump-effic-curve-harness.js`, `energy-anchor-harness.js` §10). Fixed on the way: the exporter wrote an `[ENERGY]` row naming a curve it did not write, which real EPANET refuses at the door. `lpn_energy_curve_note` now discloses only the case that is left, a curve no `[CURVES]` section defines.